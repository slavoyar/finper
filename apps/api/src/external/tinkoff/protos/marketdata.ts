/**
* This file is auto-generated by nestjs-proto-gen-ts
*/

import { Observable } from 'rxjs';
import { Metadata } from '@grpc/grpc-js';

export namespace tinkoff {
    export namespace _public {
        export namespace invest {
            export namespace api {
                export namespace contract {
                    export namespace v1 {
                        export interface MarketDataService {
                            // Запросить исторические свечи по инструменту.
                            getCandles(
                                data: GetCandlesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetCandlesResponse>;
                            // Запросить цены последних сделок по инструментам.
                            getLastPrices(
                                data: GetLastPricesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetLastPricesResponse>;
                            // Получить стакан по инструменту.
                            getOrderBook(
                                data: GetOrderBookRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetOrderBookResponse>;
                            // Запросить статус торгов по инструментам.
                            getTradingStatus(
                                data: GetTradingStatusRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetTradingStatusResponse>;
                            // Запросить статус торгов по инструментам.
                            getTradingStatuses(
                                data: GetTradingStatusesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetTradingStatusesResponse>;
                            // Запросить обезличенные сделки за последний час.
                            getLastTrades(
                                data: GetLastTradesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetLastTradesResponse>;
                            // Запросить цены закрытия торговой сессии по инструментам.
                            getClosePrices(
                                data: GetClosePricesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetClosePricesResponse>;
                            // Получить технические индикаторы по инструменту.
                            getTechAnalysis(
                                data: GetTechAnalysisRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetTechAnalysisResponse>;
                        }
                        export interface MarketDataStreamService {
                            // Bidirectional-стрим предоставления биржевой информации.
                            marketDataStream(
                                data: Observable<MarketDataRequest>,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<MarketDataResponse>;
                            // Server-side стрим предоставления биржевой информации.
                            marketDataServerSideStream(
                                data: MarketDataServerSideStreamRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<MarketDataResponse>;
                        }
                        // Запрос подписки или отписки на определенные биржевые данные.
                        export interface MarketDataRequest {
                            // Запрос подписки на свечи.
                            subscribeCandlesRequest?: v1.SubscribeCandlesRequest;
                            // Запрос подписки на стаканы.
                            subscribeOrderBookRequest?: v1.SubscribeOrderBookRequest;
                            // Запрос подписки на ленту обезличенных сделок.
                            subscribeTradesRequest?: v1.SubscribeTradesRequest;
                            // Запрос подписки на торговые статусы инструментов.
                            subscribeInfoRequest?: v1.SubscribeInfoRequest;
                            // Запрос подписки на цены последних сделок.
                            subscribeLastPriceRequest?: v1.SubscribeLastPriceRequest;
                            // Запрос своих подписок.
                            getMySubscriptions?: v1.GetMySubscriptions;
                            // Запрос проверки активности соединения.
                            ping?: v1.PingRequest;
                            // Запрос настройки пинга.
                            pingSettings?: v1.PingDelaySettings;
                        }
                        export interface MarketDataServerSideStreamRequest {
                            // Запрос подписки на свечи.
                            subscribeCandlesRequest?: v1.SubscribeCandlesRequest;
                            // Запрос подписки на стаканы.
                            subscribeOrderBookRequest?: v1.SubscribeOrderBookRequest;
                            // Запрос подписки на ленту обезличенных сделок.
                            subscribeTradesRequest?: v1.SubscribeTradesRequest;
                            // Запрос подписки на торговые статусы инструментов.
                            subscribeInfoRequest?: v1.SubscribeInfoRequest;
                            // Запрос подписки на цены последних сделок.
                            subscribeLastPriceRequest?: v1.SubscribeLastPriceRequest;
                            // Запрос настройки пинга.
                            pingSettings?: v1.PingDelaySettings;
                        }
                        // Пакет биржевой информации по подписке.
                        export interface MarketDataResponse {
                            // Результат подписки на свечи.
                            subscribeCandlesResponse?: v1.SubscribeCandlesResponse;
                            // Результат подписки на стаканы.
                            subscribeOrderBookResponse?: v1.SubscribeOrderBookResponse;
                            // Результат подписки на поток обезличенных сделок.
                            subscribeTradesResponse?: v1.SubscribeTradesResponse;
                            // Результат подписки на торговые статусы инструментов.
                            subscribeInfoResponse?: v1.SubscribeInfoResponse;
                            // Свеча.
                            candle?: v1.Candle;
                            // Сделки.
                            trade?: v1.Trade;
                            // Стакан.
                            orderbook?: v1.OrderBook;
                            // Торговый статус.
                            tradingStatus?: v1.TradingStatus;
                            // Проверка активности стрима.
                            ping?: v1.Ping;
                            // Результат подписки на цены последние сделок по инструментам.
                            subscribeLastPriceResponse?: v1.SubscribeLastPriceResponse;
                            // Цена последней сделки.
                            lastPrice?: v1.LastPrice;
                        }
                        // subscribeCandles | Изменения статуса подписки на свечи.
                        export interface SubscribeCandlesRequest {
                            // Изменение статуса подписки.
                            subscriptionAction?: v1.SubscriptionAction;
                            // Массив инструментов для подписки на свечи.
                            instruments?: v1.CandleInstrument[];
                            // Флаг ожидания закрытия временного интервала для отправки свечи.
                            waitingClose?: boolean;
                            // Источник свечей.
                            candleSourceType?: GetCandlesRequest.CandleSource;
                        }
                        // Тип операции со списком подписок.
                        export enum SubscriptionAction {
                            // Статус подписки не определен.
                            SUBSCRIPTION_ACTION_UNSPECIFIED = 0,
                            // Подписаться.
                            SUBSCRIPTION_ACTION_SUBSCRIBE = 1,
                            // Отписаться.
                            SUBSCRIPTION_ACTION_UNSUBSCRIBE = 2,
                        }
                        // Интервал свечи.
                        export enum SubscriptionInterval {
                            // Интервал свечи не определен.
                            SUBSCRIPTION_INTERVAL_UNSPECIFIED = 0,
                            // Минутные свечи.
                            SUBSCRIPTION_INTERVAL_ONE_MINUTE = 1,
                            // Пятиминутные свечи.
                            SUBSCRIPTION_INTERVAL_FIVE_MINUTES = 2,
                            // Пятнадцатиминутные свечи.
                            SUBSCRIPTION_INTERVAL_FIFTEEN_MINUTES = 3,
                            // Часовые свечи.
                            SUBSCRIPTION_INTERVAL_ONE_HOUR = 4,
                            // Дневные свечи.
                            SUBSCRIPTION_INTERVAL_ONE_DAY = 5,
                            // Двухминутные свечи.
                            SUBSCRIPTION_INTERVAL_2_MIN = 6,
                            // Трехминутные свечи.
                            SUBSCRIPTION_INTERVAL_3_MIN = 7,
                            // Десятиминутные свечи.
                            SUBSCRIPTION_INTERVAL_10_MIN = 8,
                            // Тридцатиминутные свечи.
                            SUBSCRIPTION_INTERVAL_30_MIN = 9,
                            // Двухчасовые свечи.
                            SUBSCRIPTION_INTERVAL_2_HOUR = 10,
                            // Четырехчасовые свечи.
                            SUBSCRIPTION_INTERVAL_4_HOUR = 11,
                            // Недельные свечи.
                            SUBSCRIPTION_INTERVAL_WEEK = 12,
                            // Месячные свечи.
                            SUBSCRIPTION_INTERVAL_MONTH = 13,
                        }
                        // Запрос изменения статус подписки на свечи.
                        export interface CandleInstrument {
                            // Deprecated FIGI-идентификатор инструмента. Используйте &#x60;instrument_id&#x60;.
                            figi?: string;
                            // Интервал свечей. Двухчасовые и четырехчасовые свечи в стриме отсчитываются с 0:00 по UTC.
                            interval?: v1.SubscriptionInterval;
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Результат изменения статус подписки на свечи.
                        export interface SubscribeCandlesResponse {
                            // Уникальный идентификатор запроса. [Подробнее](./grpc#tracking-id).
                            trackingId?: string;
                            // Массив статусов подписки на свечи.
                            candlesSubscriptions?: v1.CandleSubscription[];
                        }
                        // Статус подписки на свечи.
                        export interface CandleSubscription {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Интервал свечей.
                            interval?: v1.SubscriptionInterval;
                            // Статус подписки.
                            subscriptionStatus?: v1.SubscriptionStatus;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Флаг ожидания закрытия временного интервала для отправки свечи.
                            waitingClose?: boolean;
                            // Идентификатор открытого соединения.
                            streamId?: string;
                            // Идентификатор подписки в формате &#x60;UUID&#x60;.
                            subscriptionId?: string;
                            // Источник свечей.
                            candleSourceType?: GetCandlesRequest.CandleSource;
                        }
                        // Результат подписки.
                        export enum SubscriptionStatus {
                            // Статус подписки не определен.
                            SUBSCRIPTION_STATUS_UNSPECIFIED = 0,
                            // Успешно.
                            SUBSCRIPTION_STATUS_SUCCESS = 1,
                            // Инструмент не найден.
                            SUBSCRIPTION_STATUS_INSTRUMENT_NOT_FOUND = 2,
                            // Некорректный статус подписки. [Список возможных значений](./marketdata#subscriptionaction).
                            SUBSCRIPTION_STATUS_SUBSCRIPTION_ACTION_IS_INVALID = 3,
                            // Некорректная глубина стакана. Доступные значения — 1, 10, 20, 30, 40, 50.
                            SUBSCRIPTION_STATUS_DEPTH_IS_INVALID = 4,
                            // Некорректный интервал свечей. [Список возможных значений](./marketdata#subscriptioninterval).
                            SUBSCRIPTION_STATUS_INTERVAL_IS_INVALID = 5,
                            // Превышен лимит на общее количество подписок в рамках стрима. [Лимитная политика](./limits/).
                            SUBSCRIPTION_STATUS_LIMIT_IS_EXCEEDED = 6,
                            // Внутренняя ошибка сервиса.
                            SUBSCRIPTION_STATUS_INTERNAL_ERROR = 7,
                            // Превышен лимит на количество запросов на подписки в течение установленного отрезка времени.
                            SUBSCRIPTION_STATUS_TOO_MANY_REQUESTS = 8,
                            // Активная подписка не найдена. Ошибка может возникнуть только при отписке от несуществующей подписки.
                            SUBSCRIPTION_STATUS_SUBSCRIPTION_NOT_FOUND = 9,
                            // Указан некорректный источник.
                            SUBSCRIPTION_STATUS_SOURCE_IS_INVALID = 10,
                        }
                        // Запрос на изменение статуса подписки на стаканы.
                        export interface SubscribeOrderBookRequest {
                            // Изменение статуса подписки.
                            subscriptionAction?: v1.SubscriptionAction;
                            // Массив инструментов для подписки на стаканы.
                            instruments?: v1.OrderBookInstrument[];
                        }
                        // Запрос подписки на стаканы.
                        export interface OrderBookInstrument {
                            // Deprecated FIGI-идентификатор инструмента. Используйте &#x60;instrument_id&#x60;.
                            figi?: string;
                            // Глубина стакана.
                            depth?: number;
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                            // Тип стакана. Значение по умолчанию — &#x60;ORDERBOOK_TYPE_ALL&#x60;, стакан биржевой и дилера.
                            orderBookType?: v1.OrderBookType;
                        }
                        // Результат изменения статуса подписки на стаканы.
                        export interface SubscribeOrderBookResponse {
                            // Уникальный идентификатор запроса. [Подробнее](./grpc#tracking-id).
                            trackingId?: string;
                            // Массив статусов подписки на стаканы.
                            orderBookSubscriptions?: v1.OrderBookSubscription[];
                        }
                        // Статус подписки.
                        export interface OrderBookSubscription {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Глубина стакана.
                            depth?: number;
                            // Статус подписки.
                            subscriptionStatus?: v1.SubscriptionStatus;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Идентификатор открытого соединения.
                            streamId?: string;
                            // Идентификатор подписки в формате &#x60;UUID&#x60;.
                            subscriptionId?: string;
                            // Тип стакана.
                            orderBookType?: v1.OrderBookType;
                        }
                        // Типы источников сделок.
                        export enum TradeSourceType {
                            // Тип источника сделки не определён.
                            TRADE_SOURCE_UNSPECIFIED = 0,
                            // Биржевые сделки.
                            TRADE_SOURCE_EXCHANGE = 1,
                            // Сделки дилера.
                            TRADE_SOURCE_DEALER = 2,
                            // Все сделки.
                            TRADE_SOURCE_ALL = 3,
                        }
                        // Изменение статуса подписки на поток обезличенных сделок.
                        export interface SubscribeTradesRequest {
                            // Изменение статуса подписки.
                            subscriptionAction?: v1.SubscriptionAction;
                            // Массив инструментов для подписки на поток обезличенных сделок.
                            instruments?: v1.TradeInstrument[];
                            // Тип источника сделок. Значение по умолчанию — &#x60;TRADE_SOURCE_ALL&#x60;, все сделки.
                            tradeSource?: v1.TradeSourceType;
                        }
                        // Запрос подписки на поток обезличенных сделок.
                        export interface TradeInstrument {
                            // Deprecated FIGI-идентификатор инструмента. Используйте instrument_id&#x60;.
                            figi?: string;
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Результат изменения статуса подписки на поток обезличенных сделок.
                        export interface SubscribeTradesResponse {
                            // Уникальный идентификатор запроса. [Подробнее](./grpc#tracking-id).
                            trackingId?: string;
                            // Массив статусов подписки на поток сделок.
                            tradeSubscriptions?: v1.TradeSubscription[];
                            // Тип источника сделок.
                            tradeSource?: v1.TradeSourceType;
                        }
                        // Статус подписки.
                        export interface TradeSubscription {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Статус подписки.
                            subscriptionStatus?: v1.SubscriptionStatus;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Идентификатор открытого соединения.
                            streamId?: string;
                            // Идентификатор подписки в формате UUID.
                            subscriptionId?: string;
                        }
                        // Изменение статуса подписки на торговый статус инструмента.
                        export interface SubscribeInfoRequest {
                            // Изменение статуса подписки.
                            subscriptionAction?: v1.SubscriptionAction;
                            // Массив инструментов для подписки на торговый статус.
                            instruments?: v1.InfoInstrument[];
                        }
                        // Запрос подписки на торговый статус.
                        export interface InfoInstrument {
                            // Deprecated FIGI-идентификатор инструмента. Используйте instrument_id&#x60;.
                            figi?: string;
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Результат изменения статуса подписки на торговый статус.
                        export interface SubscribeInfoResponse {
                            // Уникальный идентификатор запроса. [Подробнее](./grpc#tracking-id).
                            trackingId?: string;
                            // Массив статусов подписки на торговый статус.
                            infoSubscriptions?: v1.InfoSubscription[];
                        }
                        // Статус подписки.
                        export interface InfoSubscription {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Статус подписки.
                            subscriptionStatus?: v1.SubscriptionStatus;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Идентификатор открытого соединения.
                            streamId?: string;
                            // Идентификатор подписки в формате UUID.
                            subscriptionId?: string;
                        }
                        // Изменение статуса подписки на цену последней сделки по инструменту.
                        export interface SubscribeLastPriceRequest {
                            // Изменение статуса подписки.
                            subscriptionAction?: v1.SubscriptionAction;
                            // Массив инструментов для подписки на цену последней сделки.
                            instruments?: v1.LastPriceInstrument[];
                        }
                        // Запрос подписки на последнюю цену.
                        export interface LastPriceInstrument {
                            // Deprecated FIGI-идентификатор инструмента. Используйте instrument_id&#x60;.
                            figi?: string;
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Результат изменения статуса подписки на цену последней сделки.
                        export interface SubscribeLastPriceResponse {
                            // Уникальный идентификатор запроса. [Подробнее](./grpc#tracking-id).
                            trackingId?: string;
                            // Массив статусов подписки на цену последней сделки.
                            lastPriceSubscriptions?: v1.LastPriceSubscription[];
                        }
                        // Статус подписки на цену последней сделки.
                        export interface LastPriceSubscription {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Статус подписки.
                            subscriptionStatus?: v1.SubscriptionStatus;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Идентификатор открытого соединения.
                            streamId?: string;
                            // Идентификатор подписки в формате &#x60;UUID&#x60;.
                            subscriptionId?: string;
                        }
                        // Пакет свечей в рамках стрима.
                        export interface Candle {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Интервал свечи.
                            interval?: v1.SubscriptionInterval;
                            // Цена открытия за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            open?: v1.Quotation;
                            // Максимальная цена за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            high?: v1.Quotation;
                            // Минимальная цена за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            low?: v1.Quotation;
                            // Цена закрытия за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            close?: v1.Quotation;
                            // Объем сделок в лотах.
                            volume?: number;
                            // Время начала интервала свечи по UTC.
                            time?: google.protobuf.Timestamp;
                            // Время последней сделки, вошедшей в свечу по UTC.
                            lastTradeTs?: google.protobuf.Timestamp;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Источник свечей.
                            candleSourceType?: v1.CandleSource;
                        }
                        // Пакет стаканов в рамках стрима.
                        export interface OrderBook {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Глубина стакана.
                            depth?: number;
                            // Флаг консистентности стакана. **false** — не все заявки попали в стакан из-за сетевых задержек или нарушения порядка доставки.
                            isConsistent?: boolean;
                            // Массив предложений.
                            bids?: v1.Order[];
                            // Массив спроса.
                            asks?: v1.Order[];
                            // Время формирования стакана в часовом поясе UTC по времени биржи.
                            time?: google.protobuf.Timestamp;
                            // Верхний лимит цены за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            limitUp?: v1.Quotation;
                            // Нижний лимит цены за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            limitDown?: v1.Quotation;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Тип стакана.
                            orderBookType?: v1.OrderBookType;
                        }
                        // Массив предложений/спроса.
                        export interface Order {
                            // Цена за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            price?: v1.Quotation;
                            // Количество в лотах.
                            quantity?: number;
                        }
                        // Информация о сделке.
                        export interface Trade {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Направление сделки.
                            direction?: v1.TradeDirection;
                            // Цена за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            price?: v1.Quotation;
                            // Количество лотов.
                            quantity?: number;
                            // Время сделки в часовом поясе UTC по времени биржи.
                            time?: google.protobuf.Timestamp;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Тип источника сделки.
                            tradeSource?: v1.TradeSourceType;
                        }
                        // Направление сделки.
                        export enum TradeDirection {
                            // Направление сделки не определено.
                            TRADE_DIRECTION_UNSPECIFIED = 0,
                            // Покупка.
                            TRADE_DIRECTION_BUY = 1,
                            // Продажа.
                            TRADE_DIRECTION_SELL = 2,
                        }
                        // Пакет изменения торгового статуса.
                        export interface TradingStatus {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Статус торговли инструментом.
                            tradingStatus?: v1.SecurityTradingStatus;
                            // Время изменения торгового статуса по UTC.
                            time?: google.protobuf.Timestamp;
                            // Признак доступности выставления лимитной заявки по инструменту.
                            limitOrderAvailableFlag?: boolean;
                            // Признак доступности выставления рыночной заявки по инструменту.
                            marketOrderAvailableFlag?: boolean;
                            // UID инструмента.
                            instrumentUid?: string;
                        }
                        // Запрос исторических свечей.
                        export interface GetCandlesRequest {
                            // Deprecated FIGI-идентификатор инструмента. Используйте &#x60;instrument_id&#x60;.
                            figi?: string;
                            // Начало запрашиваемого периода по UTC.
                            from?: google.protobuf.Timestamp;
                            // Окончание запрашиваемого периода по UTC.
                            to?: google.protobuf.Timestamp;
                            // Интервал запрошенных свечей.
                            interval?: v1.CandleInterval;
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                            // Тип источника свечи.
                            candleSourceType?: GetCandlesRequest.CandleSource;
                            // Максимальное количество свечей в ответе.
                            limit?: number;
                        }
                        export namespace GetCandlesRequest {
                            // Запрос исторических свечей.
                            export enum CandleSource {
                                // Все свечи.
                                CANDLE_SOURCE_UNSPECIFIED = 0,
                                // Биржевые свечи.
                                CANDLE_SOURCE_EXCHANGE = 1,
                                // Все свечи с учетом торговли по выходным.
                                CANDLE_SOURCE_INCLUDE_WEEKEND = 3,
                            }
                        }
                        // Интервал свечей. Максимальное значение интервала приведено ориентировочно, может отличаться в большую сторону в зависимости от параметров запроса.
                        export enum CandleInterval {
                            // Интервал не определен.
                            CANDLE_INTERVAL_UNSPECIFIED = 0,
                            // От 1 минуты до 1 дня. Максимальное значение &#x60;limit&#x60; — 2400.
                            CANDLE_INTERVAL_1_MIN = 1,
                            // От 5 минут до недели. Максимальное значение &#x60;limit&#x60; — 2400.
                            CANDLE_INTERVAL_5_MIN = 2,
                            // От 15 минут до 3 недель. Максимальное значение &#x60;limit&#x60; — 2400.
                            CANDLE_INTERVAL_15_MIN = 3,
                            // От 1 часа до 3 месяцев. Максимальное значение &#x60;limit&#x60; — 2400.
                            CANDLE_INTERVAL_HOUR = 4,
                            // От 1 дня до 6 лет. Максимальное значение &#x60;limit&#x60; — 2400.
                            CANDLE_INTERVAL_DAY = 5,
                            // От 2 минут до 1 дня. Максимальное значение &#x60;limit&#x60; — 1200.
                            CANDLE_INTERVAL_2_MIN = 6,
                            // От 3 минут до 1 дня. Максимальное значение &#x60;limit&#x60; — 750.
                            CANDLE_INTERVAL_3_MIN = 7,
                            // От 10 минут до недели. Максимальное значение &#x60;limit&#x60; — 1200.
                            CANDLE_INTERVAL_10_MIN = 8,
                            // От 30 минут до 3 недель. Максимальное значение &#x60;limit&#x60; — 1200.
                            CANDLE_INTERVAL_30_MIN = 9,
                            // От 2 часов до 3 месяцев. Максимальное значение &#x60;limit&#x60; — 2400.
                            CANDLE_INTERVAL_2_HOUR = 10,
                            // От 4 часов до 3 месяцев. Максимальное значение &#x60;limit&#x60; — 700.
                            CANDLE_INTERVAL_4_HOUR = 11,
                            // От 1 недели до 5 лет. Максимальное значение &#x60;limit&#x60; — 300.
                            CANDLE_INTERVAL_WEEK = 12,
                            // От 1 месяца до 10 лет. Максимальное значение &#x60;limit&#x60; — 120.
                            CANDLE_INTERVAL_MONTH = 13,
                        }
                        export enum CandleSource {
                            // Источник свечей не определен.
                            CANDLE_SOURCE_UNSPECIFIED = 0,
                            // Биржевые свечи.
                            CANDLE_SOURCE_EXCHANGE = 1,
                            // Свечи  дилера в результате торговли по выходным.
                            CANDLE_SOURCE_DEALER_WEEKEND = 2,
                        }
                        // Список свечей.
                        export interface GetCandlesResponse {
                            // Массив свечей.
                            candles?: v1.HistoricCandle[];
                        }
                        // Информация о свече.
                        export interface HistoricCandle {
                            // Цена открытия за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            open?: v1.Quotation;
                            // Максимальная цена за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            high?: v1.Quotation;
                            // Минимальная цена за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            low?: v1.Quotation;
                            // Цена закрытия за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            close?: v1.Quotation;
                            // Объем торгов в лотах.
                            volume?: number;
                            // Время свечи в часовом поясе UTC.
                            time?: google.protobuf.Timestamp;
                            // Признак завершенности свечи. **false** — свеча за текущие интервал еще сформирована не полностью.
                            isComplete?: boolean;
                            // Тип источника свечи
                            candleSource?: v1.CandleSource;
                        }
                        // Запрос получения цен последних сделок.
                        export interface GetLastPricesRequest {
                            // Deprecated FIGI-идентификатор инструмента. Используйте &#x60;instrument_id&#x60;.
                            figi?: string[];
                            // Массив идентификаторов инструмента. Принимает значения &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string[];
                            // Тип запрашиваемой последней цены.
                            lastPriceType?: v1.LastPriceType;
                            // Статус запрашиваемых инструментов. [Возможные значения](#instrumentstatus).
                            instrumentStatus?: v1.InstrumentStatus;
                        }
                        // Список цен последних сделок.
                        export interface GetLastPricesResponse {
                            // Массив цен последних сделок.
                            lastPrices?: v1.LastPrice[];
                        }
                        // Информация о цене последней сделки.
                        export interface LastPrice {
                            // FIGI инструмента.
                            figi?: string;
                            // Цена последней сделки за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            price?: v1.Quotation;
                            // Время получения последней цены в часовом поясе UTC по времени биржи.
                            time?: google.protobuf.Timestamp;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Тип последней цены.
                            lastPriceType?: v1.LastPriceType;
                        }
                        // Запрос стакана.
                        export interface GetOrderBookRequest {
                            // Deprecated FIGI-идентификатор инструмента. Используйте &#x60;instrument_id&#x60;.
                            figi?: string;
                            // Глубина стакана.
                            depth?: number;
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Информация о стакане.
                        export interface GetOrderBookResponse {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Глубина стакана.
                            depth?: number;
                            // Множество пар значений на покупку.
                            bids?: v1.Order[];
                            // Множество пар значений на продажу.
                            asks?: v1.Order[];
                            // Цена последней сделки за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            lastPrice?: v1.Quotation;
                            // Цена закрытия за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            closePrice?: v1.Quotation;
                            // Верхний лимит цены за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            limitUp?: v1.Quotation;
                            // Нижний лимит цены за 1 инструмент. Чтобы получить стоимость лота, нужно умножить на лотность инструмента. [Подробнее про перевод цен в валюту](./faq_marketdata/#_15).
                            limitDown?: v1.Quotation;
                            // Время получения цены последней сделки.
                            lastPriceTs?: google.protobuf.Timestamp;
                            // Время получения цены закрытия.
                            closePriceTs?: google.protobuf.Timestamp;
                            // Время формирования стакана на бирже.
                            orderbookTs?: google.protobuf.Timestamp;
                            // UID инструмента.
                            instrumentUid?: string;
                        }
                        // Запрос получения торгового статуса.
                        export interface GetTradingStatusRequest {
                            // Deprecated FIGI-идентификатор инструмента. Используйте &#x60;instrument_id&#x60;.
                            figi?: string;
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Запрос получения торгового статуса.
                        export interface GetTradingStatusesRequest {
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string[];
                        }
                        // Информация о торговом статусе.
                        export interface GetTradingStatusesResponse {
                            // Массив информации о торговых статусах.
                            tradingStatuses?: v1.GetTradingStatusResponse[];
                        }
                        // Информация о торговом статусе.
                        export interface GetTradingStatusResponse {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Статус торговли инструментом.
                            tradingStatus?: v1.SecurityTradingStatus;
                            // Признак доступности выставления лимитной заявки по инструменту.
                            limitOrderAvailableFlag?: boolean;
                            // Признак доступности выставления рыночной заявки по инструменту.
                            marketOrderAvailableFlag?: boolean;
                            // Признак доступности торгов через API.
                            apiTradeAvailableFlag?: boolean;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Признак доступности завяки по лучшей цене.
                            bestpriceOrderAvailableFlag?: boolean;
                            // Признак доступности только заявки по лучшей цене.
                            onlyBestPrice?: boolean;
                        }
                        // Запрос обезличенных сделок за последний час.
                        export interface GetLastTradesRequest {
                            // Deprecated FIGI-идентификатор инструмента. Используйте &#x60;instrument_id&#x60;.
                            figi?: string;
                            // Начало запрашиваемого периода по UTC.
                            from?: google.protobuf.Timestamp;
                            // Окончание запрашиваемого периода по UTC.
                            to?: google.protobuf.Timestamp;
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                            // Тип источника сделок. Значение по умолчанию — &#x60;TRADE_SOURCE_ALL&#x60;, все сделки.
                            tradeSource?: v1.TradeSourceType;
                        }
                        // Обезличенных сделок за последний час.
                        export interface GetLastTradesResponse {
                            // Массив сделок.
                            trades?: v1.Trade[];
                        }
                        // tslint:disable-next-line:no-empty-interface
                        // Запрос активных подписок. Возвращает по одному сообщению на каждый тип активных подписок — &#x60;SubscribeLastPriceResponse&#x60;, &#x60;SubscribeInfoResponse&#x60;, &#x60;SubscribeTradesResponse&#x60;, &#x60;SubscribeOrderBookResponse&#x60;, &#x60;SubscribeCandlesResponse&#x60;.
                        export interface GetMySubscriptions {
                        }
                        // Запрос цен закрытия торговой сессии по инструментам.
                        export interface GetClosePricesRequest {
                            // Массив по инструментам.
                            instruments?: v1.InstrumentClosePriceRequest[];
                            // Статус запрашиваемых инструментов. [Возможные значения](#instrumentstatus).
                            instrumentStatus?: v1.InstrumentStatus;
                        }
                        // Запрос цен закрытия торговой сессии по инструменту.
                        export interface InstrumentClosePriceRequest {
                            // Идентификатор инструмента. Принимает значение &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Цены закрытия торговой сессии по инструментам.
                        export interface GetClosePricesResponse {
                            // Массив по инструментам.
                            closePrices?: v1.InstrumentClosePriceResponse[];
                        }
                        // Цена закрытия торговой сессии по инструменту.
                        export interface InstrumentClosePriceResponse {
                            // FIGI инструмента.
                            figi?: string;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Цена закрытия торговой сессии.
                            price?: v1.Quotation;
                            // Цена последней сделки с вечерней сессии. Цена публикуется биржей по торговым дням и в нерабочие дни не обновляется.
                            eveningSessionPrice?: v1.Quotation;
                            // Дата совершения торгов.
                            time?: google.protobuf.Timestamp;
                        }
                        export interface GetTechAnalysisRequest {
                            // Тип технического индикатора.
                            indicatorType?: GetTechAnalysisRequest.IndicatorType;
                            // UID инструмента.
                            instrumentUid?: string;
                            // Начало запрашиваемого периода по UTC.
                            from?: google.protobuf.Timestamp;
                            // Окончание запрашиваемого периода по UTC.
                            to?: google.protobuf.Timestamp;
                            // Интервал, за который рассчитывается индикатор.
                            interval?: GetTechAnalysisRequest.IndicatorInterval;
                            // Тип цены, который используется при расчете индикатора.
                            typeOfPrice?: GetTechAnalysisRequest.TypeOfPrice;
                            // Торговый период, за который рассчитывается индикатор.
                            length?: number;
                            // Параметры отклонения.
                            deviation?: GetTechAnalysisRequest.Deviation;
                            // Параметры сглаживания.
                            smoothing?: GetTechAnalysisRequest.Smoothing;
                        }
                        export namespace GetTechAnalysisRequest {
                            export interface Smoothing {
                                // Короткий период сглаживания для первой экспоненциальной скользящей средней (EMA).
                                fastLength?: number;
                                // Длинный период сглаживания для второй экспоненциальной скользящей средней (EMA).
                                slowLength?: number;
                                // Период сглаживания для третьей экспоненциальной скользящей средней (EMA)
                                signalSmoothing?: number;
                            }
                            export interface Deviation {
                                // Количество стандартных отклонений, на которые отступают верхняя и нижняя границы.
                                deviationMultiplier?: v1.Quotation;
                            }
                            // Интервал свечи.
                            export enum IndicatorInterval {
                                // Интервал не определен.
                                INDICATOR_INTERVAL_UNSPECIFIED = 0,
                                // 1 минута.
                                INDICATOR_INTERVAL_ONE_MINUTE = 1,
                                // 5 минут.
                                INDICATOR_INTERVAL_FIVE_MINUTES = 2,
                                // 15 минут.
                                INDICATOR_INTERVAL_FIFTEEN_MINUTES = 3,
                                // 1 час.
                                INDICATOR_INTERVAL_ONE_HOUR = 4,
                                // 1 день.
                                INDICATOR_INTERVAL_ONE_DAY = 5,
                                // 2 минуты.
                                INDICATOR_INTERVAL_2_MIN = 6,
                                // 3 минуты.
                                INDICATOR_INTERVAL_3_MIN = 7,
                                // 10 минут.
                                INDICATOR_INTERVAL_10_MIN = 8,
                                // 30 минут.
                                INDICATOR_INTERVAL_30_MIN = 9,
                                // 2 часа.
                                INDICATOR_INTERVAL_2_HOUR = 10,
                                // 4 часа.
                                INDICATOR_INTERVAL_4_HOUR = 11,
                                // Неделя.
                                INDICATOR_INTERVAL_WEEK = 12,
                                // Месяц.
                                INDICATOR_INTERVAL_MONTH = 13,
                            }
                            export enum TypeOfPrice {
                                // Не указано.
                                TYPE_OF_PRICE_UNSPECIFIED = 0,
                                // Цена закрытия.
                                TYPE_OF_PRICE_CLOSE = 1,
                                // Цена открытия.
                                TYPE_OF_PRICE_OPEN = 2,
                                // Максимальное значение за выбранный интервал.
                                TYPE_OF_PRICE_HIGH = 3,
                                // Минимальное значение за выбранный интервал.
                                TYPE_OF_PRICE_LOW = 4,
                                // Среднее значение по показателям [ (close + open + high + low) / 4 ].
                                TYPE_OF_PRICE_AVG = 5,
                            }
                            export enum IndicatorType {
                                // Не определен.
                                INDICATOR_TYPE_UNSPECIFIED = 0,
                                // Bollinger Bands — линия Боллинжера.
                                INDICATOR_TYPE_BB = 1,
                                // Exponential Moving Average — EMA, экспоненциальная скользящая средняя.
                                INDICATOR_TYPE_EMA = 2,
                                // Relative Strength Index — индекс относительной силы.
                                INDICATOR_TYPE_RSI = 3,
                                // Moving Average Convergence/Divergence — схождение/расхождение скользящих средних.
                                INDICATOR_TYPE_MACD = 4,
                                // Simple Moving Average — простое скользящее среднее.
                                INDICATOR_TYPE_SMA = 5,
                            }
                        }
                        export interface GetTechAnalysisResponse {
                            // Массив значений результатов технического анализа.
                            technicalIndicators?: GetTechAnalysisResponse.TechAnalysisItem[];
                        }
                        export namespace GetTechAnalysisResponse {
                            export interface TechAnalysisItem {
                                // Временная метка по UTC, для которой были рассчитаны значения индикатора.
                                timestamp?: google.protobuf.Timestamp;
                                // Значение простого скользящего среднего (средней линии).
                                middleBand?: v1.Quotation;
                                // Значение верхней линии Боллинджера.
                                upperBand?: v1.Quotation;
                                // Значение нижней линии Боллинджера.
                                lowerBand?: v1.Quotation;
                                // Значение сигнальной линии.
                                signal?: v1.Quotation;
                                // Значение линии MACD.
                                macd?: v1.Quotation;
                            }
                        }
                        export enum OrderBookType {
                            // Не определен.
                            ORDERBOOK_TYPE_UNSPECIFIED = 0,
                            // Биржевой стакан.
                            ORDERBOOK_TYPE_EXCHANGE = 1,
                            // Стакан дилера.
                            ORDERBOOK_TYPE_DEALER = 2,
                            // Стакан биржевой и дилера.
                            ORDERBOOK_TYPE_ALL = 3,
                        }
                        // Тип последней цены
                        export enum LastPriceType {
                            // Не определен.
                            LAST_PRICE_UNSPECIFIED = 0,
                            // Цена биржи.
                            LAST_PRICE_EXCHANGE = 1,
                            // Цена дилера
                            LAST_PRICE_DEALER = 2,
                        }
                        // Тип инструмента.
                        export enum InstrumentType {
                            INSTRUMENT_TYPE_UNSPECIFIED = 0,
                            // Облигация.
                            INSTRUMENT_TYPE_BOND = 1,
                            // Акция.
                            INSTRUMENT_TYPE_SHARE = 2,
                            // Валюта.
                            INSTRUMENT_TYPE_CURRENCY = 3,
                            // Exchange-traded fund. Фонд.
                            INSTRUMENT_TYPE_ETF = 4,
                            // Фьючерс.
                            INSTRUMENT_TYPE_FUTURES = 5,
                            // Структурная нота.
                            INSTRUMENT_TYPE_SP = 6,
                            // Опцион.
                            INSTRUMENT_TYPE_OPTION = 7,
                            // Clearing certificate.
                            INSTRUMENT_TYPE_CLEARING_CERTIFICATE = 8,
                            // Индекс.
                            INSTRUMENT_TYPE_INDEX = 9,
                            // Товар.
                            INSTRUMENT_TYPE_COMMODITY = 10,
                        }
                        // Статус запрашиваемых инструментов.
                        export enum InstrumentStatus {
                            // Значение не определено.
                            INSTRUMENT_STATUS_UNSPECIFIED = 0,
                            // По умолчанию — базовый список инструментов, которыми можно торговать через T-Invest API. Сейчас списки доступных бумаг в API и других интерфейсах совпадают — кроме внебиржевых бумаг, но в будущем списки могут различаться.
                            INSTRUMENT_STATUS_BASE = 1,
                            // Список всех инструментов.
                            INSTRUMENT_STATUS_ALL = 2,
                        }
                        // Денежная сумма в определенной валюте.
                        export interface MoneyValue {
                            // Строковый ISO-код валюты.
                            currency?: string;
                            // Целая часть суммы, может быть отрицательным числом.
                            units?: number;
                            // Дробная часть суммы, может быть отрицательным числом.
                            nano?: number;
                        }
                        // Котировка — денежная сумма без указания валюты.
                        export interface Quotation {
                            // Целая часть суммы, может быть отрицательным числом.
                            units?: number;
                            // Дробная часть суммы, может быть отрицательным числом.
                            nano?: number;
                        }
                        // Режим торгов инструмента
                        export enum SecurityTradingStatus {
                            // Торговый статус не определен.
                            SECURITY_TRADING_STATUS_UNSPECIFIED = 0,
                            // Недоступен для торгов.
                            SECURITY_TRADING_STATUS_NOT_AVAILABLE_FOR_TRADING = 1,
                            // Период открытия торгов.
                            SECURITY_TRADING_STATUS_OPENING_PERIOD = 2,
                            // Период закрытия торгов.
                            SECURITY_TRADING_STATUS_CLOSING_PERIOD = 3,
                            // Перерыв в торговле.
                            SECURITY_TRADING_STATUS_BREAK_IN_TRADING = 4,
                            // Нормальная торговля.
                            SECURITY_TRADING_STATUS_NORMAL_TRADING = 5,
                            // Аукцион закрытия.
                            SECURITY_TRADING_STATUS_CLOSING_AUCTION = 6,
                            // Аукцион крупных пакетов.
                            SECURITY_TRADING_STATUS_DARK_POOL_AUCTION = 7,
                            // Дискретный аукцион.
                            SECURITY_TRADING_STATUS_DISCRETE_AUCTION = 8,
                            // Аукцион открытия.
                            SECURITY_TRADING_STATUS_OPENING_AUCTION_PERIOD = 9,
                            // Период торгов по цене аукциона закрытия.
                            SECURITY_TRADING_STATUS_TRADING_AT_CLOSING_AUCTION_PRICE = 10,
                            // Сессия назначена.
                            SECURITY_TRADING_STATUS_SESSION_ASSIGNED = 11,
                            // Сессия закрыта.
                            SECURITY_TRADING_STATUS_SESSION_CLOSE = 12,
                            // Сессия открыта.
                            SECURITY_TRADING_STATUS_SESSION_OPEN = 13,
                            // Доступна торговля в режиме внутренней ликвидности брокера.
                            SECURITY_TRADING_STATUS_DEALER_NORMAL_TRADING = 14,
                            // Перерыв торговли в режиме внутренней ликвидности брокера.
                            SECURITY_TRADING_STATUS_DEALER_BREAK_IN_TRADING = 15,
                            // Недоступна торговля в режиме внутренней ликвидности брокера.
                            SECURITY_TRADING_STATUS_DEALER_NOT_AVAILABLE_FOR_TRADING = 16,
                        }
                        export interface PingRequest {
                            // Время формирования запроса.
                            time?: google.protobuf.Timestamp;
                        }
                        export interface PingDelaySettings {
                            // Задержка (пинг) сообщений:  5000–180 000 миллисекунд. Значение по умолчанию — 120 000.
                            pingDelayMs?: number;
                        }
                        // Проверка активности стрима.
                        export interface Ping {
                            // Время проверки.
                            time?: google.protobuf.Timestamp;
                            // Идентификатор соединения.
                            streamId?: string;
                            // Время формирования запроса.
                            pingRequestTime?: google.protobuf.Timestamp;
                        }
                        // Тип цены.
                        export enum PriceType {
                            // Значение не определено.
                            PRICE_TYPE_UNSPECIFIED = 0,
                            // Цена в пунктах (только для фьючерсов и облигаций).
                            PRICE_TYPE_POINT = 1,
                            // Цена в валюте расчетов по инструменту.
                            PRICE_TYPE_CURRENCY = 2,
                        }
                        export interface Page {
                            // Максимальное число возвращаемых записей.
                            limit?: number;
                            // Порядковый номер страницы, начиная с 0.
                            pageNumber?: number;
                        }
                        export interface PageResponse {
                            // Максимальное число возвращаемых записей.
                            limit?: number;
                            // Порядковый номер страницы, начиная с 0.
                            pageNumber?: number;
                            // Общее количество записей.
                            totalCount?: number;
                        }
                        export interface ResponseMetadata {
                            // Идентификатор трекинга.
                            trackingId?: string;
                            // Серверное время.
                            serverTime?: google.protobuf.Timestamp;
                        }
                        export interface BrandData {
                            // Логотип инструмента. Имя файла для получения логотипа.
                            logoName?: string;
                            // Цвет бренда.
                            logoBaseColor?: string;
                            // Цвет текста для цвета логотипа бренда.
                            textColor?: string;
                        }
                        export enum ResultSubscriptionStatus {
                            // Статус подписки не определен.
                            RESULT_SUBSCRIPTION_STATUS_UNSPECIFIED = 0,
                            // Подписка успешно установлена.
                            RESULT_SUBSCRIPTION_STATUS_OK = 1,
                            // Ошибка подписки
                            RESULT_SUBSCRIPTION_STATUS_ERROR = 13,
                        }
                        export interface ErrorDetail {
                            // Код ошибки.
                            code?: string;
                            // Описание ошибки.
                            message?: string;
                        }
                    }
                }
            }
        }
    }
}
export namespace google {
    export namespace protobuf {
        export interface Timestamp {
            seconds?: number;
            nanos?: number;
        }
    }
}

