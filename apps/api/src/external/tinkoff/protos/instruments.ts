/**
* This file is auto-generated by nestjs-proto-gen-ts
*/

import { Observable } from 'rxjs';
import { Metadata } from '@grpc/grpc-js';

export namespace tinkoff {
    export namespace _public {
        export namespace invest {
            export namespace api {
                export namespace contract {
                    export namespace v1 {
                        export interface InstrumentsService {
                            // Получить расписания торгов торговых площадок
                            tradingSchedules(
                                data: TradingSchedulesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<TradingSchedulesResponse>;
                            // Получить облигации по ее идентификатору
                            bondBy(
                                data: InstrumentRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<BondResponse>;
                            // Получить список облигаций
                            bonds(
                                data: InstrumentsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<BondsResponse>;
                            // Получить график выплат купонов по облигации
                            getBondCoupons(
                                data: GetBondCouponsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetBondCouponsResponse>;
                            // Получить события по облигации
                            getBondEvents(
                                data: GetBondEventsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetBondEventsResponse>;
                            // Получить валюту по ее идентификатору
                            currencyBy(
                                data: InstrumentRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<CurrencyResponse>;
                            // Получить список валют
                            currencies(
                                data: InstrumentsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<CurrenciesResponse>;
                            // Получить инвестиционный фонд по его идентификатору
                            etfBy(
                                data: InstrumentRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<EtfResponse>;
                            // Получить список инвестиционных фондов
                            etfs(
                                data: InstrumentsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<EtfsResponse>;
                            // Получить фьючерс по его идентификатору
                            futureBy(
                                data: InstrumentRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<FutureResponse>;
                            // Получить список фьючерсов
                            futures(
                                data: InstrumentsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<FuturesResponse>;
                            // Получить опцион по его идентификатору
                            optionBy(
                                data: InstrumentRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<OptionResponse>;
                            // Deprecated Получить список опционов
                            options(
                                data: InstrumentsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<OptionsResponse>;
                            // Получить список опционов
                            optionsBy(
                                data: FilterOptionsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<OptionsResponse>;
                            // Получить акцию по ее идентификатору
                            shareBy(
                                data: InstrumentRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<ShareResponse>;
                            // Получить список акций
                            shares(
                                data: InstrumentsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<SharesResponse>;
                            // Получить индикативные инструменты — индексы, товары и другие
                            indicatives(
                                data: IndicativesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<IndicativesResponse>;
                            // Получить накопленный купонный доход по облигации
                            getAccruedInterests(
                                data: GetAccruedInterestsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetAccruedInterestsResponse>;
                            // Получить размера гарантийного обеспечения по фьючерсам
                            getFuturesMargin(
                                data: GetFuturesMarginRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetFuturesMarginResponse>;
                            // Получить основную информацию об инструменте
                            getInstrumentBy(
                                data: InstrumentRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<InstrumentResponse>;
                            // Получить события выплаты дивидендов по инструменту
                            getDividends(
                                data: GetDividendsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetDividendsResponse>;
                            // Получить актив по его идентификатору
                            getAssetBy(
                                data: AssetRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<AssetResponse>;
                            // Получить список активов. Метод работает для всех инструментов, кроме срочных — фьючерсов и опционов
                            getAssets(
                                data: AssetsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<AssetsResponse>;
                            // Получить список избранных инструментов
                            getFavorites(
                                data: GetFavoritesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetFavoritesResponse>;
                            // Отредактировать список избранных инструментов
                            editFavorites(
                                data: EditFavoritesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<EditFavoritesResponse>;
                            // Получить список стран
                            getCountries(
                                data: GetCountriesRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetCountriesResponse>;
                            // Найти инструмент
                            findInstrument(
                                data: FindInstrumentRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<FindInstrumentResponse>;
                            // Получить список брендов
                            getBrands(
                                data: GetBrandsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetBrandsResponse>;
                            // Получить бренд по его идентификатору
                            getBrandBy(
                                data: GetBrandRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<Brand>;
                            // Получить фундаментальные показатели по активу
                            getAssetFundamentals(
                                data: GetAssetFundamentalsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetAssetFundamentalsResponse>;
                            // Получить расписания выхода отчетностей эмитентов
                            getAssetReports(
                                data: GetAssetReportsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetAssetReportsResponse>;
                            // Получить мнения аналитиков по инструменту
                            getConsensusForecasts(
                                data: GetConsensusForecastsRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetConsensusForecastsResponse>;
                            // Получить прогнозов инвестдомов по инструменту
                            getForecastBy(
                                data: GetForecastRequest,
                                metadata?: Metadata,
                                ...rest: any[]
                            ): Observable<GetForecastResponse>;
                        }
                        // Запрос расписания торгов.
                        export interface TradingSchedulesRequest {
                            // Наименование биржи или расчетного календаря. &lt;br/&gt;Если не передается, возвращается информация по всем доступным торговым площадкам.
                            exchange?: string;
                            // Начало периода по UTC.
                            from?: google.protobuf.Timestamp;
                            // Окончание периода по UTC.
                            to?: google.protobuf.Timestamp;
                        }
                        // Список торговых площадок.
                        export interface TradingSchedulesResponse {
                            // Список торговых площадок и режимов торгов.
                            exchanges?: v1.TradingSchedule[];
                        }
                        // Данные по торговой площадке.
                        export interface TradingSchedule {
                            // Наименование торговой площадки.
                            exchange?: string;
                            // Массив с торговыми и неторговыми днями.
                            days?: v1.TradingDay[];
                        }
                        // Информация о времени торгов.
                        export interface TradingDay {
                            // Дата.
                            date?: google.protobuf.Timestamp;
                            // Признак торгового дня на бирже.
                            isTradingDay?: boolean;
                            // Время начала торгов по UTC.
                            startTime?: google.protobuf.Timestamp;
                            // Время окончания торгов по UTC.
                            endTime?: google.protobuf.Timestamp;
                            // Время начала аукциона открытия по UTC.
                            openingAuctionStartTime?: google.protobuf.Timestamp;
                            // Время окончания аукциона закрытия по UTC.
                            closingAuctionEndTime?: google.protobuf.Timestamp;
                            // Время начала аукциона открытия вечерней сессии по UTC.
                            eveningOpeningAuctionStartTime?: google.protobuf.Timestamp;
                            // Время начала вечерней сессии по UTC.
                            eveningStartTime?: google.protobuf.Timestamp;
                            // Время окончания вечерней сессии по UTC.
                            eveningEndTime?: google.protobuf.Timestamp;
                            // Время начала основного клиринга по UTC.
                            clearingStartTime?: google.protobuf.Timestamp;
                            // Время окончания основного клиринга по UTC.
                            clearingEndTime?: google.protobuf.Timestamp;
                            // Время начала премаркета по UTC.
                            premarketStartTime?: google.protobuf.Timestamp;
                            // Время окончания премаркета по UTC.
                            premarketEndTime?: google.protobuf.Timestamp;
                            // Время начала аукциона закрытия по UTC.
                            closingAuctionStartTime?: google.protobuf.Timestamp;
                            // Время окончания аукциона открытия по UTC.
                            openingAuctionEndTime?: google.protobuf.Timestamp;
                            // Торговые интервалы.
                            intervals?: v1.TradingInterval[];
                        }
                        // Запрос получения инструмента по идентификатору.
                        export interface InstrumentRequest {
                            // Тип идентификатора инструмента. Возможные значения — &#x60;figi&#x60;, &#x60;ticker&#x60;. [Подробнее об идентификации инструментов](./faq_identification/).
                            idType?: v1.InstrumentIdType;
                            // Идентификатор &#x60;class_code&#x60;. Обязательный, если &#x60;id_type &#x3D; ticker&#x60;.
                            classCode?: string;
                            // Идентификатор запрашиваемого инструмента.
                            id?: string;
                        }
                        // Запрос получения инструментов.
                        export interface InstrumentsRequest {
                            // Статус запрашиваемых инструментов. [Возможные значения](#instrumentstatus).
                            instrumentStatus?: v1.InstrumentStatus;
                            // Тип площадки торговли. [Возможные значения](#instrumentexchangetype).
                            instrumentExchange?: v1.InstrumentExchangeType;
                        }
                        // Параметры фильтрации опционов.
                        export interface FilterOptionsRequest {
                            // Идентификатор базового актива опциона.  Обязательный параметр.
                            basicAssetUid?: string;
                            // Идентификатор позиции базового актива опциона.
                            basicAssetPositionUid?: string;
                        }
                        // Информация об облигации.
                        export interface BondResponse {
                            // Информация об облигации.
                            instrument?: v1.Bond;
                        }
                        // Список облигаций.
                        export interface BondsResponse {
                            // Массив облигаций.
                            instruments?: v1.Bond[];
                        }
                        // Запрос купонов по облигации.
                        export interface GetBondCouponsRequest {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Начало запрашиваемого периода по UTC. Фильтрация по &#x60;coupon_date&#x60; — дата выплаты купона.
                            from?: google.protobuf.Timestamp;
                            // Окончание запрашиваемого периода по UTC. Фильтрация по &#x60;coupon_date&#x60; — дата выплаты купона.
                            to?: google.protobuf.Timestamp;
                            // Идентификатор инструмента — &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Купоны по облигации.
                        export interface GetBondCouponsResponse {
                            events?: v1.Coupon[];
                        }
                        // События по облигации.
                        export interface GetBondEventsRequest {
                            // Начало запрашиваемого периода по UTC.
                            from?: google.protobuf.Timestamp;
                            // Окончание запрашиваемого периода по UTC.
                            to?: google.protobuf.Timestamp;
                            // Идентификатор инструмента — &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                            // Тип события
                            type?: GetBondEventsRequest.EventType;
                        }
                        export namespace GetBondEventsRequest {
                            // События по облигации.
                            export enum EventType {
                                // Неопределенное значение.
                                EVENT_TYPE_UNSPECIFIED = 0,
                                // Купон.
                                EVENT_TYPE_CPN = 1,
                                // Опцион (оферта).
                                EVENT_TYPE_CALL = 2,
                                // Погашение.
                                EVENT_TYPE_MTY = 3,
                                // Конвертация.
                                EVENT_TYPE_CONV = 4,
                            }
                        }
                        // Объект передачи информации о событии облигации.
                        export interface GetBondEventsResponse {
                            events?: GetBondEventsResponse.BondEvent[];
                        }
                        export namespace GetBondEventsResponse {
                            // Объект передачи информации о событии облигации.
                            export interface BondEvent {
                                // Идентификатор инструмента.
                                instrumentId?: string;
                                // Номер события для данного типа события.
                                eventNumber?: number;
                                // Дата события.
                                eventDate?: google.protobuf.Timestamp;
                                // Тип события.
                                eventType?: GetBondEventsRequest.EventType;
                                // Полное количество бумаг, задействованных в событии.
                                eventTotalVol?: v1.Quotation;
                                // Дата фиксации владельцев для участия в событии.
                                fixDate?: google.protobuf.Timestamp;
                                // Дата определения даты или факта события.
                                rateDate?: google.protobuf.Timestamp;
                                // Дата дефолта, если применимо.
                                defaultDate?: google.protobuf.Timestamp;
                                // Дата реального исполнения обязательства.
                                realPayDate?: google.protobuf.Timestamp;
                                // Дата выплаты.
                                payDate?: google.protobuf.Timestamp;
                                // Выплата на одну облигацию.
                                payOneBond?: v1.MoneyValue;
                                // Выплаты на все бумаги, задействованные в событии.
                                moneyFlowVal?: v1.MoneyValue;
                                // Признак исполнения.
                                execution?: string;
                                // Тип операции.
                                operationType?: string;
                                // Стоимость операции — ставка купона, доля номинала, цена выкупа или коэффициент конвертации.
                                value?: v1.Quotation;
                                // Примечание.
                                note?: string;
                                // ID выпуска бумаг, в который произведена конвертация (для конвертаций).
                                convertToFinToolId?: string;
                                // Начало купонного периода.
                                couponStartDate?: google.protobuf.Timestamp;
                                // Окончание купонного периода.
                                couponEndDate?: google.protobuf.Timestamp;
                                // Купонный период.
                                couponPeriod?: number;
                                // Ставка купона, процентов годовых.
                                couponInterestRate?: v1.Quotation;
                            }
                        }
                        // Объект передачи информации о купоне облигации.
                        export interface Coupon {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Дата выплаты купона.
                            couponDate?: google.protobuf.Timestamp;
                            // Номер купона.
                            couponNumber?: number;
                            // Дата фиксации реестра для выплаты купона — опционально.
                            fixDate?: google.protobuf.Timestamp;
                            // Выплата на одну облигацию.
                            payOneBond?: v1.MoneyValue;
                            // Тип купона.
                            couponType?: v1.CouponType;
                            // Начало купонного периода.
                            couponStartDate?: google.protobuf.Timestamp;
                            // Окончание купонного периода.
                            couponEndDate?: google.protobuf.Timestamp;
                            // Купонный период в днях.
                            couponPeriod?: number;
                        }
                        // Тип купонов.
                        export enum CouponType {
                            // Неопределенное значение.
                            COUPON_TYPE_UNSPECIFIED = 0,
                            // Постоянный.
                            COUPON_TYPE_CONSTANT = 1,
                            // Плавающий.
                            COUPON_TYPE_FLOATING = 2,
                            // Дисконт.
                            COUPON_TYPE_DISCOUNT = 3,
                            // Ипотечный.
                            COUPON_TYPE_MORTGAGE = 4,
                            // Фиксированный.
                            COUPON_TYPE_FIX = 5,
                            // Переменный.
                            COUPON_TYPE_VARIABLE = 6,
                            // Прочее.
                            COUPON_TYPE_OTHER = 7,
                        }
                        // Данные по валюте.
                        export interface CurrencyResponse {
                            // Информация о валюте.
                            instrument?: v1.Currency;
                        }
                        // Данные по валютам.
                        export interface CurrenciesResponse {
                            // Массив валют.
                            instruments?: v1.Currency[];
                        }
                        // Данные по фонду.
                        export interface EtfResponse {
                            // Информация о фонде.
                            instrument?: v1.Etf;
                        }
                        // Данные по фондам.
                        export interface EtfsResponse {
                            // Массив фондов.
                            instruments?: v1.Etf[];
                        }
                        // Данные по фьючерсу.
                        export interface FutureResponse {
                            // Информация о фьючерсу.
                            instrument?: v1.Future;
                        }
                        // Данные по фьючерсам.
                        export interface FuturesResponse {
                            // Массив фьючерсов.
                            instruments?: v1.Future[];
                        }
                        // Данные по опциону.
                        export interface OptionResponse {
                            // Информация по опциону.
                            instrument?: v1.Option;
                        }
                        // Данные по опционам.
                        export interface OptionsResponse {
                            // Массив данных по опциону.
                            instruments?: v1.Option[];
                        }
                        // Опцион.
                        export interface Option {
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Уникальный идентификатор позиции.
                            positionUid?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код.
                            classCode?: string;
                            // Уникальный идентификатор позиции основного инструмента.
                            basicAssetPositionUid?: string;
                            // Текущий режим торгов инструмента.
                            tradingStatus?: v1.SecurityTradingStatus;
                            // Реальная площадка исполнения расчетов (биржа).
                            realExchange?: v1.RealExchange;
                            // Направление опциона.
                            direction?: v1.OptionDirection;
                            // Тип расчетов по опциону.
                            paymentType?: v1.OptionPaymentType;
                            // Стиль опциона.
                            style?: v1.OptionStyle;
                            // Способ исполнения опциона.
                            settlementType?: v1.OptionSettlementType;
                            // Название инструмента.
                            name?: string;
                            // Валюта.
                            currency?: string;
                            // Валюта, в которой оценивается контракт.
                            settlementCurrency?: string;
                            // Тип актива.
                            assetType?: string;
                            // Основной актив.
                            basicAsset?: string;
                            // Tорговая площадка (секция биржи).
                            exchange?: string;
                            // Код страны рисков.
                            countryOfRisk?: string;
                            // Наименование страны рисков.
                            countryOfRiskName?: string;
                            // Сектор экономики.
                            sector?: string;
                            // Информация о бренде.
                            brand?: v1.BrandData;
                            // Количество бумаг в лоте.
                            lot?: number;
                            // Размер основного актива.
                            basicAssetSize?: v1.Quotation;
                            // Коэффициент ставки риска длинной позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            klong?: v1.Quotation;
                            // Коэффициент ставки риска короткой позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            kshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlong?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongMin?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortMin?: v1.Quotation;
                            // Минимальный шаг цены.
                            minPriceIncrement?: v1.Quotation;
                            // Цена страйка.
                            strikePrice?: v1.MoneyValue;
                            // Ставка риска в лонг с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongClient?: v1.Quotation;
                            // Ставка риска в шорт с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortClient?: v1.Quotation;
                            // Дата истечения срока в формате UTC.
                            expirationDate?: google.protobuf.Timestamp;
                            // Дата начала обращения контракта в формате UTC.
                            firstTradeDate?: google.protobuf.Timestamp;
                            // Дата исполнения в формате UTC.
                            lastTradeDate?: google.protobuf.Timestamp;
                            // Дата первой минутной свечи в формате UTC.
                            first_1minCandleDate?: google.protobuf.Timestamp;
                            // Дата первой дневной свечи в формате UTC.
                            first_1dayCandleDate?: google.protobuf.Timestamp;
                            // Признак доступности для операций шорт.
                            shortEnabledFlag?: boolean;
                            // Возможность покупки или продажи на ИИС.
                            forIisFlag?: boolean;
                            // Флаг, используемый ранее для определения внебиржевых инструментов. На данный момент не используется для торгуемых через API инструментов. Может использоваться как фильтр для операций, совершавшихся некоторое время назад на ОТС площадке.
                            otcFlag?: boolean;
                            // Признак доступности для покупки.
                            buyAvailableFlag?: boolean;
                            // Признак доступности для продажи.
                            sellAvailableFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом только для квалифицированных инвесторов.
                            forQualInvestorFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом по выходным.
                            weekendFlag?: boolean;
                            // Флаг заблокированного ТКС.
                            blockedTcaFlag?: boolean;
                            // Возможность торговать инструментом через API.
                            apiTradeAvailableFlag?: boolean;
                        }
                        // Тип опциона по направлению сделки.
                        export enum OptionDirection {
                            // Тип не определен.
                            OPTION_DIRECTION_UNSPECIFIED = 0,
                            // Опцион на продажу.
                            OPTION_DIRECTION_PUT = 1,
                            // Опцион на покупку.
                            OPTION_DIRECTION_CALL = 2,
                        }
                        // Тип расчетов по опциону.
                        export enum OptionPaymentType {
                            // Тип не определен.
                            OPTION_PAYMENT_TYPE_UNSPECIFIED = 0,
                            // Опционы с использованием премии в расчетах.
                            OPTION_PAYMENT_TYPE_PREMIUM = 1,
                            // Маржируемые опционы.
                            OPTION_PAYMENT_TYPE_MARGINAL = 2,
                        }
                        // Тип опциона по стилю.
                        export enum OptionStyle {
                            // Тип не определен.
                            OPTION_STYLE_UNSPECIFIED = 0,
                            // Американский опцион.
                            OPTION_STYLE_AMERICAN = 1,
                            // Европейский опцион.
                            OPTION_STYLE_EUROPEAN = 2,
                        }
                        // Тип опциона по способу исполнения.
                        export enum OptionSettlementType {
                            // Тип не определен.
                            OPTION_EXECUTION_TYPE_UNSPECIFIED = 0,
                            // Поставочный тип опциона.
                            OPTION_EXECUTION_TYPE_PHYSICAL_DELIVERY = 1,
                            // Расчетный тип опциона.
                            OPTION_EXECUTION_TYPE_CASH_SETTLEMENT = 2,
                        }
                        // Данные по акции.
                        export interface ShareResponse {
                            // Информация об акции.
                            instrument?: v1.Share;
                        }
                        // Данные по акциям.
                        export interface SharesResponse {
                            // Массив акций.
                            instruments?: v1.Share[];
                        }
                        // Объект передачи информации об облигации.
                        export interface Bond {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код (секция торгов).
                            classCode?: string;
                            // ISIN-идентификатор инструмента.
                            isin?: string;
                            // Лотность инструмента. Возможно совершение операций только на количества ценной бумаги, кратные параметру &#x60;lot&#x60;. [Подробнее](./glossary#lot).
                            lot?: number;
                            // Валюта расчетов.
                            currency?: string;
                            // Коэффициент ставки риска длинной позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            klong?: v1.Quotation;
                            // Коэффициент ставки риска короткой позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            kshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlong?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongMin?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortMin?: v1.Quotation;
                            // Признак доступности для операций в шорт.
                            shortEnabledFlag?: boolean;
                            // Название инструмента.
                            name?: string;
                            // Tорговая площадка (секция биржи).
                            exchange?: string;
                            // Количество выплат по купонам в год.
                            couponQuantityPerYear?: number;
                            // Дата погашения облигации по UTC.
                            maturityDate?: google.protobuf.Timestamp;
                            // Номинал облигации.
                            nominal?: v1.MoneyValue;
                            // Первоначальный номинал облигации.
                            initialNominal?: v1.MoneyValue;
                            // Дата выпуска облигации по UTC.
                            stateRegDate?: google.protobuf.Timestamp;
                            // Дата размещения по UTC.
                            placementDate?: google.protobuf.Timestamp;
                            // Цена размещения.
                            placementPrice?: v1.MoneyValue;
                            // Значение НКД (накопленного купонного дохода) на дату.
                            aciValue?: v1.MoneyValue;
                            // Код страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRisk?: string;
                            // Наименование страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRiskName?: string;
                            // Сектор экономики.
                            sector?: string;
                            // Форма выпуска. Возможные значения: &lt;br/&gt;**documentary** — документарная; &lt;br/&gt;**non_documentary** — бездокументарная.
                            issueKind?: string;
                            // Размер выпуска.
                            issueSize?: number;
                            // Плановый размер выпуска.
                            issueSizePlan?: number;
                            // Текущий режим торгов инструмента.
                            tradingStatus?: v1.SecurityTradingStatus;
                            // Флаг, используемый ранее для определения внебиржевых инструментов. На данный момент не используется для торгуемых через API инструментов. Может использоваться как фильтр для операций, совершавшихся некоторое время назад на ОТС площадке.
                            otcFlag?: boolean;
                            // Признак доступности для покупки.
                            buyAvailableFlag?: boolean;
                            // Признак доступности для продажи.
                            sellAvailableFlag?: boolean;
                            // Признак облигации с плавающим купоном.
                            floatingCouponFlag?: boolean;
                            // Признак бессрочной облигации.
                            perpetualFlag?: boolean;
                            // Признак облигации с амортизацией долга.
                            amortizationFlag?: boolean;
                            // Шаг цены.
                            minPriceIncrement?: v1.Quotation;
                            // Параметр указывает на возможность торговать инструментом через API.
                            apiTradeAvailableFlag?: boolean;
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Реальная площадка исполнения расчетов. (биржа)
                            realExchange?: v1.RealExchange;
                            // Уникальный идентификатор позиции инструмента.
                            positionUid?: string;
                            // Уникальный идентификатор актива.
                            assetUid?: string;
                            // Признак доступности для ИИС.
                            forIisFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом только для квалифицированных инвесторов.
                            forQualInvestorFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом по выходным.
                            weekendFlag?: boolean;
                            // Флаг заблокированного ТКС.
                            blockedTcaFlag?: boolean;
                            // Признак субординированной облигации.
                            subordinatedFlag?: boolean;
                            // Флаг достаточной ликвидности.
                            liquidityFlag?: boolean;
                            // Дата первой минутной свечи.
                            first_1minCandleDate?: google.protobuf.Timestamp;
                            // Дата первой дневной свечи.
                            first_1dayCandleDate?: google.protobuf.Timestamp;
                            // Уровень риска.
                            riskLevel?: v1.RiskLevel;
                            // Информация о бренде.
                            brand?: v1.BrandData;
                            // Тип облигации.
                            bondType?: v1.BondType;
                            // Дата погашения облигации.
                            callDate?: google.protobuf.Timestamp;
                            // Ставка риска в лонг с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongClient?: v1.Quotation;
                            // Ставка риска в шорт с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortClient?: v1.Quotation;
                        }
                        // Объект передачи информации о валюте.
                        export interface Currency {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код (секция торгов).
                            classCode?: string;
                            // ISIN-идентификатор инструмента.
                            isin?: string;
                            // Лотность инструмента. Возможно совершение операций только на количества ценной бумаги, кратные параметру &#x60;lot&#x60;. [Подробнее](./glossary#lot).
                            lot?: number;
                            // Валюта расчетов.
                            currency?: string;
                            // Коэффициент ставки риска длинной позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            klong?: v1.Quotation;
                            // Коэффициент ставки риска короткой позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            kshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlong?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongMin?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortMin?: v1.Quotation;
                            // Признак доступности для операций в шорт.
                            shortEnabledFlag?: boolean;
                            // Название инструмента.
                            name?: string;
                            // Tорговая площадка (секция биржи).
                            exchange?: string;
                            // Номинал.
                            nominal?: v1.MoneyValue;
                            // Код страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRisk?: string;
                            // Наименование страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRiskName?: string;
                            // Текущий режим торгов инструмента.
                            tradingStatus?: v1.SecurityTradingStatus;
                            // Флаг, используемый ранее для определения внебиржевых инструментов. На данный момент не используется для торгуемых через API инструментов. Может использоваться как фильтр для операций, совершавшихся некоторое время назад на ОТС площадке.
                            otcFlag?: boolean;
                            // Признак доступности для покупки.
                            buyAvailableFlag?: boolean;
                            // Признак доступности для продажи.
                            sellAvailableFlag?: boolean;
                            // Строковый ISO-код валюты.
                            isoCurrencyName?: string;
                            // Шаг цены.
                            minPriceIncrement?: v1.Quotation;
                            // Параметр указывает на возможность торговать инструментом через API.
                            apiTradeAvailableFlag?: boolean;
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Реальная площадка исполнения расчетов (биржа).
                            realExchange?: v1.RealExchange;
                            // Уникальный идентификатор позиции инструмента.
                            positionUid?: string;
                            // Признак доступности для ИИС.
                            forIisFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом только для квалифицированных инвесторов.
                            forQualInvestorFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом по выходным.
                            weekendFlag?: boolean;
                            // Флаг заблокированного ТКС.
                            blockedTcaFlag?: boolean;
                            // Дата первой минутной свечи.
                            first_1minCandleDate?: google.protobuf.Timestamp;
                            // Дата первой дневной свечи.
                            first_1dayCandleDate?: google.protobuf.Timestamp;
                            // Информация о бренде.
                            brand?: v1.BrandData;
                            // Ставка риска в лонг с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongClient?: v1.Quotation;
                            // Ставка риска в шорт с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortClient?: v1.Quotation;
                        }
                        // Объект передачи информации об инвестиционном фонде.
                        export interface Etf {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код (секция торгов).
                            classCode?: string;
                            // ISIN-идентификатор инструмента.
                            isin?: string;
                            // Лотность инструмента. Возможно совершение операций только на количества ценной бумаги, кратные параметру &#x60;lot&#x60;. [Подробнее](./glossary#lot).
                            lot?: number;
                            // Валюта расчетов.
                            currency?: string;
                            // Коэффициент ставки риска длинной позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            klong?: v1.Quotation;
                            // Коэффициент ставки риска короткой позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            kshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlong?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongMin?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortMin?: v1.Quotation;
                            // Признак доступности для операций в шорт.
                            shortEnabledFlag?: boolean;
                            // Название инструмента.
                            name?: string;
                            // Tорговая площадка (секция биржи).
                            exchange?: string;
                            // Размер фиксированной комиссии фонда.
                            fixedCommission?: v1.Quotation;
                            // Возможные значения: &lt;br/&gt;**equity** — акции;&lt;br/&gt;**fixed_income** — облигации;&lt;br/&gt;**mixed_allocation** — смешанный;&lt;br/&gt;**money_market** — денежный рынок;&lt;br/&gt;**real_estate** — недвижимость;&lt;br/&gt;**commodity** — товары;&lt;br/&gt;**specialty** — специальный;&lt;br/&gt;**private_equity** — private equity;&lt;br/&gt;**alternative_investment** — альтернативные инвестиции.
                            focusType?: string;
                            // Дата выпуска по UTC.
                            releasedDate?: google.protobuf.Timestamp;
                            // Количество паев фонда в обращении.
                            numShares?: v1.Quotation;
                            // Код страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRisk?: string;
                            // Наименование страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRiskName?: string;
                            // Сектор экономики.
                            sector?: string;
                            // Частота ребалансировки.
                            rebalancingFreq?: string;
                            // Текущий режим торгов инструмента.
                            tradingStatus?: v1.SecurityTradingStatus;
                            // Флаг, используемый ранее для определения внебиржевых инструментов. На данный момент не используется для торгуемых через API инструментов. Может использоваться как фильтр для операций, совершавшихся некоторое время назад на ОТС площадке.
                            otcFlag?: boolean;
                            // Признак доступности для покупки.
                            buyAvailableFlag?: boolean;
                            // Признак доступности для продажи.
                            sellAvailableFlag?: boolean;
                            // Шаг цены.
                            minPriceIncrement?: v1.Quotation;
                            // Параметр указывает на возможность торговать инструментом через API.
                            apiTradeAvailableFlag?: boolean;
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Реальная площадка исполнения расчетов (биржа).
                            realExchange?: v1.RealExchange;
                            // Уникальный идентификатор позиции инструмента.
                            positionUid?: string;
                            // Уникальный идентификатор актива.
                            assetUid?: string;
                            // Тип площадки торговли.
                            instrumentExchange?: v1.InstrumentExchangeType;
                            // Признак доступности для ИИС.
                            forIisFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом только для квалифицированных инвесторов.
                            forQualInvestorFlag?: boolean;
                            // ФлагФлаг, отображающий доступность торговли инструментом по выходным.
                            weekendFlag?: boolean;
                            // Флаг заблокированного ТКС.
                            blockedTcaFlag?: boolean;
                            // Флаг достаточной ликвидности.
                            liquidityFlag?: boolean;
                            // Дата первой минутной свечи.
                            first_1minCandleDate?: google.protobuf.Timestamp;
                            // Дата первой дневной свечи.
                            first_1dayCandleDate?: google.protobuf.Timestamp;
                            // Информация о бренде.
                            brand?: v1.BrandData;
                            // Ставка риска в лонг с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongClient?: v1.Quotation;
                            // Ставка риска в шорт с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortClient?: v1.Quotation;
                        }
                        // Объект передачи информации о фьючерсе.
                        export interface Future {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код (секция торгов).
                            classCode?: string;
                            // Лотность инструмента. Возможно совершение операций только на количества ценной бумаги, кратные параметру &#x60;lot&#x60;. [Подробнее](./glossary#lot).
                            lot?: number;
                            // Валюта расчетов.
                            currency?: string;
                            // Коэффициент ставки риска длинной позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            klong?: v1.Quotation;
                            // Коэффициент ставки риска короткой позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            kshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlong?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongMin?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortMin?: v1.Quotation;
                            // Признак доступности для операций шорт.
                            shortEnabledFlag?: boolean;
                            // Название инструмента.
                            name?: string;
                            // Tорговая площадка (секция биржи).
                            exchange?: string;
                            // Дата начала обращения контракта по UTC.
                            firstTradeDate?: google.protobuf.Timestamp;
                            // Дата по UTC, до которой возможно проведение операций с фьючерсом.
                            lastTradeDate?: google.protobuf.Timestamp;
                            // Тип фьючерса. Возможные значения: &lt;br/&gt;**physical_delivery** — физические поставки; &lt;br/&gt;**cash_settlement** — денежный эквивалент.
                            futuresType?: string;
                            // Тип актива. Возможные значения: &lt;br/&gt;**commodity** — товар; &lt;br/&gt;**currency** — валюта; &lt;br/&gt;**security** — ценная бумага; &lt;br/&gt;**index** — индекс.
                            assetType?: string;
                            // Основной актив.
                            basicAsset?: string;
                            // Размер основного актива.
                            basicAssetSize?: v1.Quotation;
                            // Код страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRisk?: string;
                            // Наименование страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRiskName?: string;
                            // Сектор экономики.
                            sector?: string;
                            // Дата истечения срока в часов поясе UTC.
                            expirationDate?: google.protobuf.Timestamp;
                            // Текущий режим торгов инструмента.
                            tradingStatus?: v1.SecurityTradingStatus;
                            // Флаг, используемый ранее для определения внебиржевых инструментов. На данный момент не используется для торгуемых через API инструментов. Может использоваться как фильтр для операций, совершавшихся некоторое время назад на ОТС площадке.
                            otcFlag?: boolean;
                            // Признак доступности для покупки.
                            buyAvailableFlag?: boolean;
                            // Признак доступности для продажи.
                            sellAvailableFlag?: boolean;
                            // Шаг цены.
                            minPriceIncrement?: v1.Quotation;
                            // Параметр указывает на возможность торговать инструментом через API.
                            apiTradeAvailableFlag?: boolean;
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Реальная площадка исполнения расчетов (биржа).
                            realExchange?: v1.RealExchange;
                            // Уникальный идентификатор позиции инструмента.
                            positionUid?: string;
                            // Уникальный идентификатор позиции основного инструмента.
                            basicAssetPositionUid?: string;
                            // Признак доступности для ИИС.
                            forIisFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом только для квалифицированных инвесторов.
                            forQualInvestorFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом по выходным.
                            weekendFlag?: boolean;
                            // Флаг заблокированного ТКС.
                            blockedTcaFlag?: boolean;
                            // Дата первой минутной свечи.
                            first_1minCandleDate?: google.protobuf.Timestamp;
                            // Дата первой дневной свечи.
                            first_1dayCandleDate?: google.protobuf.Timestamp;
                            // Гарантийное обеспечение при покупке.
                            initialMarginOnBuy?: v1.MoneyValue;
                            // Гарантийное обеспечение при продаже.
                            initialMarginOnSell?: v1.MoneyValue;
                            // Стоимость шага цены.
                            minPriceIncrementAmount?: v1.Quotation;
                            // Информация о бренде.
                            brand?: v1.BrandData;
                            // Ставка риска в лонг с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongClient?: v1.Quotation;
                            // Ставка риска в шорт с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortClient?: v1.Quotation;
                        }
                        // Объект передачи информации об акции.
                        export interface Share {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код (секция торгов).
                            classCode?: string;
                            // ISIN-идентификатор инструмента.
                            isin?: string;
                            // Лотность инструмента. Возможно совершение операций только на количества ценной бумаги, кратные параметру &#x60;lot&#x60;. [Подробнее](./glossary#lot)
                            lot?: number;
                            // Валюта расчетов.
                            currency?: string;
                            // Коэффициент ставки риска длинной позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            klong?: v1.Quotation;
                            // Коэффициент ставки риска короткой позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            kshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlong?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongMin?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortMin?: v1.Quotation;
                            // Признак доступности для операций в шорт.
                            shortEnabledFlag?: boolean;
                            // Название инструмента.
                            name?: string;
                            // Tорговая площадка (секция биржи).
                            exchange?: string;
                            // Дата IPO акции по UTC.
                            ipoDate?: google.protobuf.Timestamp;
                            // Размер выпуска.
                            issueSize?: number;
                            // Код страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRisk?: string;
                            // Наименование страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRiskName?: string;
                            // Сектор экономики.
                            sector?: string;
                            // Плановый размер выпуска.
                            issueSizePlan?: number;
                            // Номинал.
                            nominal?: v1.MoneyValue;
                            // Текущий режим торгов инструмента.
                            tradingStatus?: v1.SecurityTradingStatus;
                            // Флаг, используемый ранее для определения внебиржевых инструментов. На данный момент не используется для торгуемых через API инструментов. Может использоваться как фильтр для операций, совершавшихся некоторое время назад на ОТС площадке.
                            otcFlag?: boolean;
                            // Признак доступности для покупки.
                            buyAvailableFlag?: boolean;
                            // Признак доступности для продажи.
                            sellAvailableFlag?: boolean;
                            // Признак наличия дивидендной доходности.
                            divYieldFlag?: boolean;
                            // Тип акции. Возможные значения — &#x60;[ShareType](./instruments#sharetype)&#x60;.
                            shareType?: v1.ShareType;
                            // Шаг цены.
                            minPriceIncrement?: v1.Quotation;
                            // Возможность торговать инструментом через API.
                            apiTradeAvailableFlag?: boolean;
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Реальная площадка исполнения расчетов (биржа).
                            realExchange?: v1.RealExchange;
                            // Уникальный идентификатор позиции инструмента.
                            positionUid?: string;
                            // Уникальный идентификатор актива.
                            assetUid?: string;
                            // Тип площадки торговли.
                            instrumentExchange?: v1.InstrumentExchangeType;
                            // Признак доступности для ИИС.
                            forIisFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом только для квалифицированных инвесторов.
                            forQualInvestorFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом по выходным.
                            weekendFlag?: boolean;
                            // Флаг заблокированного ТКС.
                            blockedTcaFlag?: boolean;
                            // Флаг достаточной ликвидности.
                            liquidityFlag?: boolean;
                            // Дата первой минутной свечи.
                            first_1minCandleDate?: google.protobuf.Timestamp;
                            // Дата первой дневной свечи.
                            first_1dayCandleDate?: google.protobuf.Timestamp;
                            // Информация о бренде.
                            brand?: v1.BrandData;
                            // Ставка риска в лонг с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongClient?: v1.Quotation;
                            // Ставка риска в шорт с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortClient?: v1.Quotation;
                        }
                        // Запрос НКД по облигации.
                        export interface GetAccruedInterestsRequest {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Начало запрашиваемого периода по UTC.
                            from?: google.protobuf.Timestamp;
                            // Окончание запрашиваемого периода по UTC.
                            to?: google.protobuf.Timestamp;
                            // Идентификатор инструмента — &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // НКД облигации.
                        export interface GetAccruedInterestsResponse {
                            // Массив операций начисления купонов.
                            accruedInterests?: v1.AccruedInterest[];
                        }
                        // Операция начисления купонов.
                        export interface AccruedInterest {
                            // Дата и время выплаты по UTC.
                            date?: google.protobuf.Timestamp;
                            // Величина выплаты.
                            value?: v1.Quotation;
                            // Величина выплаты в процентах от номинала.
                            valuePercent?: v1.Quotation;
                            // Номинал облигации.
                            nominal?: v1.Quotation;
                        }
                        // Запрос информации о фьючерсе
                        export interface GetFuturesMarginRequest {
                            // Идентификатор инструмента.
                            figi?: string;
                            // Идентификатор инструмента — &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Данные по фьючерсу
                        export interface GetFuturesMarginResponse {
                            // Гарантийное обеспечение при покупке.
                            initialMarginOnBuy?: v1.MoneyValue;
                            // Гарантийное обеспечение при продаже.
                            initialMarginOnSell?: v1.MoneyValue;
                            // Шаг цены.
                            minPriceIncrement?: v1.Quotation;
                            // Стоимость шага цены.
                            minPriceIncrementAmount?: v1.Quotation;
                        }
                        // Тип идентификатора инструмента. [Подробнее об идентификации инструментов](./faq_identification/).
                        export enum InstrumentIdType {
                            // Значение не определено.
                            INSTRUMENT_ID_UNSPECIFIED = 0,
                            // FIGI.
                            INSTRUMENT_ID_TYPE_FIGI = 1,
                            // Ticker.
                            INSTRUMENT_ID_TYPE_TICKER = 2,
                            // Уникальный идентификатор.
                            INSTRUMENT_ID_TYPE_UID = 3,
                            // Идентификатор позиции.
                            INSTRUMENT_ID_TYPE_POSITION_UID = 4,
                        }
                        // Данные по инструменту.
                        export interface InstrumentResponse {
                            // Основная информация об инструменте.
                            instrument?: v1.Instrument;
                        }
                        // Объект передачи основной информации об инструменте.
                        export interface Instrument {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код инструмента.
                            classCode?: string;
                            // ISIN-идентификатор инструмента.
                            isin?: string;
                            // Лотность инструмента. Возможно совершение операций только на количества ценной бумаги, кратные параметру &#x60;lot&#x60;. [Подробнее](./glossary#lot).
                            lot?: number;
                            // Валюта расчетов.
                            currency?: string;
                            // Коэффициент ставки риска длинной позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            klong?: v1.Quotation;
                            // Коэффициент ставки риска короткой позиции по клиенту. 2 – клиент со стандартным уровнем риска (КСУР); 1 – клиент с повышенным уровнем риска (КПУР).
                            kshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlong?: v1.Quotation;
                            // Ставка риска начальной маржи для КСУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshort?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР лонг. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongMin?: v1.Quotation;
                            // Ставка риска начальной маржи для КПУР шорт. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortMin?: v1.Quotation;
                            // Признак доступности для операций в шорт.
                            shortEnabledFlag?: boolean;
                            // Название инструмента.
                            name?: string;
                            // Tорговая площадка (секция биржи).
                            exchange?: string;
                            // Код страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRisk?: string;
                            // Наименование страны риска — то есть страны, в которой компания ведет основной бизнес.
                            countryOfRiskName?: string;
                            // Тип инструмента.
                            instrumentType?: string;
                            // Текущий режим торгов инструмента.
                            tradingStatus?: v1.SecurityTradingStatus;
                            // Флаг, используемый ранее для определения внебиржевых инструментов. На данный момент не используется для торгуемых через API инструментов. Может использоваться как фильтр для операций, совершавшихся некоторое время назад на ОТС площадке.
                            otcFlag?: boolean;
                            // Признак доступности для покупки.
                            buyAvailableFlag?: boolean;
                            // Признак доступности для продажи.
                            sellAvailableFlag?: boolean;
                            // Шаг цены.
                            minPriceIncrement?: v1.Quotation;
                            // Параметр указывает на возможность торговать инструментом через API.
                            apiTradeAvailableFlag?: boolean;
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Реальная площадка исполнения расчетов (биржа).
                            realExchange?: v1.RealExchange;
                            // Уникальный идентификатор позиции инструмента.
                            positionUid?: string;
                            // Уникальный идентификатор актива.
                            assetUid?: string;
                            // Признак доступности для ИИС.
                            forIisFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом только для квалифицированных инвесторов.
                            forQualInvestorFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом по выходным.
                            weekendFlag?: boolean;
                            // Флаг заблокированного ТКС.
                            blockedTcaFlag?: boolean;
                            // Тип инструмента.
                            instrumentKind?: v1.InstrumentType;
                            // Дата первой минутной свечи.
                            first_1minCandleDate?: google.protobuf.Timestamp;
                            // Дата первой дневной свечи.
                            first_1dayCandleDate?: google.protobuf.Timestamp;
                            // Информация о бренде.
                            brand?: v1.BrandData;
                            // Ставка риска в лонг с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dlongClient?: v1.Quotation;
                            // Ставка риска в шорт с учетом текущего уровня риска портфеля клиента. [Подробнее про ставки риска](https://www.tbank.ru/invest/help/brokerage/account/margin/about/#q5).
                            dshortClient?: v1.Quotation;
                        }
                        // Запрос дивидендов.
                        export interface GetDividendsRequest {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Начало запрашиваемого периода по UTC. Фильтрация происходит по параметру &#x60;record_date&#x60; — дата фиксации реестра.
                            from?: google.protobuf.Timestamp;
                            // Окончание запрашиваемого периода по UTC. Фильтрация происходит по параметру &#x60;record_date&#x60; — дата фиксации реестра.
                            to?: google.protobuf.Timestamp;
                            // Идентификатор инструмента — &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Дивиденды.
                        export interface GetDividendsResponse {
                            dividends?: v1.Dividend[];
                        }
                        // Информация о выплате.
                        export interface Dividend {
                            // Величина дивиденда на 1 ценную бумагу (включая валюту).
                            dividendNet?: v1.MoneyValue;
                            // Дата фактических выплат по UTC.
                            paymentDate?: google.protobuf.Timestamp;
                            // Дата объявления дивидендов по UTC.
                            declaredDate?: google.protobuf.Timestamp;
                            // Последний день (включительно) покупки для получения выплаты по UTC.
                            lastBuyDate?: google.protobuf.Timestamp;
                            // Тип выплаты. Возможные значения: &#x60;Regular Cash&#x60; – регулярные выплаты, &#x60;Cancelled&#x60; – выплата отменена, &#x60;Daily Accrual&#x60; – ежедневное начисление, &#x60;Return of Capital&#x60; – возврат капитала, прочие типы выплат.
                            dividendType?: string;
                            // Дата фиксации реестра по UTC.
                            recordDate?: google.protobuf.Timestamp;
                            // Регулярность выплаты. Возможные значения: &#x60;Annual&#x60; – ежегодная, &#x60;Semi-Anl&#x60; – каждые полгода, прочие типы выплат.
                            regularity?: string;
                            // Цена закрытия инструмента на момент &#x60;ex_dividend_date&#x60;.
                            closePrice?: v1.MoneyValue;
                            // Величина доходности.
                            yieldValue?: v1.Quotation;
                            // Дата и время создания записи по UTC.
                            createdAt?: google.protobuf.Timestamp;
                        }
                        // Тип акций.
                        export enum ShareType {
                            // Значение не определено.
                            SHARE_TYPE_UNSPECIFIED = 0,
                            // Обыкновенная.
                            SHARE_TYPE_COMMON = 1,
                            // Привилегированная.
                            SHARE_TYPE_PREFERRED = 2,
                            // Американские депозитарные расписки.
                            SHARE_TYPE_ADR = 3,
                            // Глобальные депозитарные расписки.
                            SHARE_TYPE_GDR = 4,
                            // Товарищество с ограниченной ответственностью.
                            SHARE_TYPE_MLP = 5,
                            // Акции из реестра Нью-Йорка.
                            SHARE_TYPE_NY_REG_SHRS = 6,
                            // Закрытый инвестиционный фонд.
                            SHARE_TYPE_CLOSED_END_FUND = 7,
                            // Траст недвижимости.
                            SHARE_TYPE_REIT = 8,
                        }
                        // Запрос актива по идентификатору.
                        export interface AssetRequest {
                            // UID-идентификатор актива.
                            id?: string;
                        }
                        // Данные по активу.
                        export interface AssetResponse {
                            // Актив.
                            asset?: v1.AssetFull;
                        }
                        // Запрос списка активов.
                        export interface AssetsRequest {
                            instrumentType?: v1.InstrumentType;
                            // Статус запрашиваемых инструментов. [Возможные значения](#instrumentstatus).
                            instrumentStatus?: v1.InstrumentStatus;
                        }
                        // Список активов.
                        export interface AssetsResponse {
                            // Активы.
                            assets?: v1.Asset[];
                        }
                        export interface AssetFull {
                            // Уникальный идентификатор актива.
                            uid?: string;
                            // Тип актива.
                            type?: v1.AssetType;
                            // Наименование актива.
                            name?: string;
                            // Короткое наименование актива.
                            nameBrief?: string;
                            // Описание актива.
                            description?: string;
                            // Дата и время удаления актива.
                            deletedAt?: google.protobuf.Timestamp;
                            // Тестирование клиентов.
                            requiredTests?: string[];
                            // Валюта. Обязательно и заполняется только для &#x60;type &#x3D; ASSET_TYPE_CURRENCY&#x60;.
                            currency?: v1.AssetCurrency;
                            // Ценная бумага. Обязательно и заполняется только для &#x60;type &#x3D; ASSET_TYPE_SECURITY&#x60;.
                            security?: v1.AssetSecurity;
                            // Номер государственной регистрации.
                            gosRegCode?: string;
                            // Код CFI.
                            cfi?: string;
                            // Код НРД инструмента.
                            codeNsd?: string;
                            // Статус актива.
                            status?: string;
                            // Бренд.
                            brand?: v1.Brand;
                            // Дата и время последнего обновления записи.
                            updatedAt?: google.protobuf.Timestamp;
                            // Код типа ц.б. по классификации Банка России.
                            brCode?: string;
                            // Наименование кода типа ц.б. по классификации Банка России.
                            brCodeName?: string;
                            // Массив идентификаторов инструментов.
                            instruments?: v1.AssetInstrument[];
                        }
                        // Информация об активе.
                        export interface Asset {
                            // Уникальный идентификатор актива.
                            uid?: string;
                            // Тип актива.
                            type?: v1.AssetType;
                            // Наименование актива.
                            name?: string;
                            // Массив идентификаторов инструментов.
                            instruments?: v1.AssetInstrument[];
                        }
                        // Тип актива.
                        export enum AssetType {
                            // Тип не определен.
                            ASSET_TYPE_UNSPECIFIED = 0,
                            // Валюта.
                            ASSET_TYPE_CURRENCY = 1,
                            // Товар.
                            ASSET_TYPE_COMMODITY = 2,
                            // Индекс.
                            ASSET_TYPE_INDEX = 3,
                            // Ценная бумага.
                            ASSET_TYPE_SECURITY = 4,
                        }
                        // Валюта.
                        export interface AssetCurrency {
                            // ISO-код валюты.
                            baseCurrency?: string;
                        }
                        // Ценная бумага.
                        export interface AssetSecurity {
                            // ISIN-идентификатор ценной бумаги.
                            isin?: string;
                            // Тип ценной бумаги.
                            type?: string;
                            // Тип инструмента.
                            instrumentKind?: v1.InstrumentType;
                            // Акция. Заполняется только для акций — тип актива &#x60;asset.type &#x3D; ASSET_TYPE_SECURITY&#x60; и &#x60;security.type &#x3D; share&#x60;.
                            share?: v1.AssetShare;
                            // Облигация. Заполняется только для облигаций — тип актива &#x60;asset.type &#x3D; ASSET_TYPE_SECURITY&#x60; и &#x60;security.type &#x3D; bond&#x60;.
                            bond?: v1.AssetBond;
                            // Структурная нота. Заполняется только для структурных продуктов — тип актива &#x60;asset.type &#x3D; ASSET_TYPE_SECURITY&#x60; и &#x60;security.type &#x3D; sp&#x60;.
                            sp?: v1.AssetStructuredProduct;
                            // Фонд. Заполняется только для фондов — тип актива &#x60;asset.type &#x3D; ASSET_TYPE_SECURITY&#x60; и &#x60;security.type &#x3D; etf&#x60;.
                            etf?: v1.AssetEtf;
                            // Клиринговый сертификат участия. Заполняется только для клиринговых сертификатов — тип актива &#x60;asset.type &#x3D; ASSET_TYPE_SECURITY&#x60; и security.type &#x3D; &#x60;clearing_certificate&#x60;.
                            clearingCertificate?: v1.AssetClearingCertificate;
                        }
                        // Акция.
                        export interface AssetShare {
                            // Тип акции.
                            type?: v1.ShareType;
                            // Объем выпуска (шт.).
                            issueSize?: v1.Quotation;
                            // Номинал.
                            nominal?: v1.Quotation;
                            // Валюта номинала.
                            nominalCurrency?: string;
                            // Индекс (Bloomberg).
                            primaryIndex?: string;
                            // Ставка дивиденда (для привилегированных акций).
                            dividendRate?: v1.Quotation;
                            // Тип привилегированных акций.
                            preferredShareType?: string;
                            // Дата IPO.
                            ipoDate?: google.protobuf.Timestamp;
                            // Дата регистрации.
                            registryDate?: google.protobuf.Timestamp;
                            // Признак наличия дивидендной доходности.
                            divYieldFlag?: boolean;
                            // Форма выпуска ФИ.
                            issueKind?: string;
                            // Дата размещения акции.
                            placementDate?: google.protobuf.Timestamp;
                            // ISIN базового актива.
                            represIsin?: string;
                            // Объявленное количество, шт.
                            issueSizePlan?: v1.Quotation;
                            // Количество акций в свободном обращении.
                            totalFloat?: v1.Quotation;
                        }
                        // Облигация.
                        export interface AssetBond {
                            // Текущий номинал.
                            currentNominal?: v1.Quotation;
                            // Наименование заемщика.
                            borrowName?: string;
                            // Объем эмиссии облигации (стоимость).
                            issueSize?: v1.Quotation;
                            // Номинал облигации.
                            nominal?: v1.Quotation;
                            // Валюта номинала.
                            nominalCurrency?: string;
                            // Форма выпуска облигации.
                            issueKind?: string;
                            // Форма дохода облигации.
                            interestKind?: string;
                            // Количество выплат в год.
                            couponQuantityPerYear?: number;
                            // Признак облигации с индексируемым номиналом.
                            indexedNominalFlag?: boolean;
                            // Признак субординированной облигации.
                            subordinatedFlag?: boolean;
                            // Признак обеспеченной облигации.
                            collateralFlag?: boolean;
                            // Признак показывает, что купоны облигации не облагаются налогом — для mass market.
                            taxFreeFlag?: boolean;
                            // Признак облигации с амортизацией долга.
                            amortizationFlag?: boolean;
                            // Признак облигации с плавающим купоном.
                            floatingCouponFlag?: boolean;
                            // Признак бессрочной облигации.
                            perpetualFlag?: boolean;
                            // Дата погашения облигации.
                            maturityDate?: google.protobuf.Timestamp;
                            // Описание и условия получения дополнительного дохода.
                            returnCondition?: string;
                            // Дата выпуска облигации.
                            stateRegDate?: google.protobuf.Timestamp;
                            // Дата размещения облигации.
                            placementDate?: google.protobuf.Timestamp;
                            // Цена размещения облигации.
                            placementPrice?: v1.Quotation;
                            // Объявленное количество, шт.
                            issueSizePlan?: v1.Quotation;
                        }
                        // Структурная нота.
                        export interface AssetStructuredProduct {
                            // Наименование заемщика.
                            borrowName?: string;
                            // Номинал.
                            nominal?: v1.Quotation;
                            // Валюта номинала.
                            nominalCurrency?: string;
                            // Тип структурной ноты.
                            type?: v1.StructuredProductType;
                            // Стратегия портфеля.
                            logicPortfolio?: string;
                            // Тип базового актива.
                            assetType?: v1.AssetType;
                            // Вид базового актива в зависимости от типа базового актива.
                            basicAsset?: string;
                            // Барьер сохранности в процентах.
                            safetyBarrier?: v1.Quotation;
                            // Дата погашения.
                            maturityDate?: google.protobuf.Timestamp;
                            // Объявленное количество, шт.
                            issueSizePlan?: v1.Quotation;
                            // Объем размещения.
                            issueSize?: v1.Quotation;
                            // Дата размещения ноты.
                            placementDate?: google.protobuf.Timestamp;
                            // Форма выпуска.
                            issueKind?: string;
                        }
                        // Тип структурной ноты.
                        export enum StructuredProductType {
                            // Тип не определен.
                            SP_TYPE_UNSPECIFIED = 0,
                            // Поставочный.
                            SP_TYPE_DELIVERABLE = 1,
                            // Беспоставочный.
                            SP_TYPE_NON_DELIVERABLE = 2,
                        }
                        // Фонд.
                        export interface AssetEtf {
                            // Суммарные расходы фонда в процентах.
                            totalExpense?: v1.Quotation;
                            // Барьерная ставка доходности, после которой фонд имеет право на perfomance fee — в процентах.
                            hurdleRate?: v1.Quotation;
                            // Комиссия за успешные результаты фонда в процентах.
                            performanceFee?: v1.Quotation;
                            // Фиксированная комиссия за управление в процентах.
                            fixedCommission?: v1.Quotation;
                            // Тип распределения доходов от выплат по бумагам.
                            paymentType?: string;
                            // Признак необходимости выхода фонда в плюс для получения комиссии.
                            watermarkFlag?: boolean;
                            // Премия (надбавка к цене) при покупке доли в фонде — в процентах.
                            buyPremium?: v1.Quotation;
                            // Ставка дисконта (вычет из цены) при продаже доли в фонде — в процентах.
                            sellDiscount?: v1.Quotation;
                            // Признак ребалансируемости портфеля фонда.
                            rebalancingFlag?: boolean;
                            // Периодичность ребалансировки.
                            rebalancingFreq?: string;
                            // Тип управления.
                            managementType?: string;
                            // Индекс, который реплицирует (старается копировать) фонд.
                            primaryIndex?: string;
                            // База ETF.
                            focusType?: string;
                            // Признак использования заемных активов (плечо).
                            leveragedFlag?: boolean;
                            // Количество акций в обращении.
                            numShare?: v1.Quotation;
                            // Признак обязательства по отчетности перед регулятором.
                            ucitsFlag?: boolean;
                            // Дата выпуска.
                            releasedDate?: google.protobuf.Timestamp;
                            // Описание фонда.
                            description?: string;
                            // Описание индекса, за которым следует фонд.
                            primaryIndexDescription?: string;
                            // Основные компании, в которые вкладывается фонд.
                            primaryIndexCompany?: string;
                            // Срок восстановления индекса после просадки.
                            indexRecoveryPeriod?: v1.Quotation;
                            // IVAV-код.
                            inavCode?: string;
                            // Признак наличия дивидендной доходности.
                            divYieldFlag?: boolean;
                            // Комиссия на покрытие расходов фонда в процентах.
                            expenseCommission?: v1.Quotation;
                            // Ошибка следования за индексом в процентах.
                            primaryIndexTrackingError?: v1.Quotation;
                            // Плановая ребалансировка портфеля.
                            rebalancingPlan?: string;
                            // Ставки налогообложения дивидендов и купонов.
                            taxRate?: string;
                            // Даты ребалансировок.
                            rebalancingDates?: google.protobuf.Timestamp[];
                            // Форма выпуска.
                            issueKind?: string;
                            // Номинал.
                            nominal?: v1.Quotation;
                            // Валюта номинала.
                            nominalCurrency?: string;
                        }
                        // Клиринговый сертификат участия.
                        export interface AssetClearingCertificate {
                            // Номинал.
                            nominal?: v1.Quotation;
                            // Валюта номинала.
                            nominalCurrency?: string;
                        }
                        // Бренд.
                        export interface Brand {
                            // UID-идентификатор бренда.
                            uid?: string;
                            // Наименование бренда.
                            name?: string;
                            // Описание.
                            description?: string;
                            // Информация о бренде.
                            info?: string;
                            // Компания.
                            company?: string;
                            // Сектор.
                            sector?: string;
                            // Код страны риска.
                            countryOfRisk?: string;
                            // Наименование страны риска.
                            countryOfRiskName?: string;
                        }
                        // Идентификаторы инструмента.
                        export interface AssetInstrument {
                            // UID-идентификатор инструмента.
                            uid?: string;
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Тип инструмента.
                            instrumentType?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код (секция торгов).
                            classCode?: string;
                            // Массив связанных инструментов.
                            links?: v1.InstrumentLink[];
                            // Тип инструмента.
                            instrumentKind?: v1.InstrumentType;
                            // ID позиции.
                            positionUid?: string;
                        }
                        // Связь с другим инструментом.
                        export interface InstrumentLink {
                            // Тип связи.
                            type?: string;
                            // UID-идентификатор связанного инструмента.
                            instrumentUid?: string;
                        }
                        // tslint:disable-next-line:no-empty-interface
                        // Запрос списка избранных инструментов, входные параметры не требуются.
                        export interface GetFavoritesRequest {
                        }
                        // В ответ передается список избранных инструментов в качестве массива.
                        export interface GetFavoritesResponse {
                            // Массив инструментов.
                            favoriteInstruments?: v1.FavoriteInstrument[];
                        }
                        // Массив избранных инструментов.
                        export interface FavoriteInstrument {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код инструмента.
                            classCode?: string;
                            // ISIN-идентификатор инструмента.
                            isin?: string;
                            // Тип инструмента.
                            instrumentType?: string;
                            // Название инструмента.
                            name?: string;
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Флаг, используемый ранее для определения внебиржевых инструментов. На данный момент не используется для торгуемых через API инструментов. Может использоваться как фильтр для операций, совершавшихся некоторое время назад на ОТС площадке.
                            otcFlag?: boolean;
                            // Возможность торговать инструментом через API.
                            apiTradeAvailableFlag?: boolean;
                            // Тип инструмента.
                            instrumentKind?: v1.InstrumentType;
                        }
                        // Запрос редактирования списка избранных инструментов.
                        export interface EditFavoritesRequest {
                            // Массив инструментов.
                            instruments?: v1.EditFavoritesRequestInstrument[];
                            // Тип действия со списком.
                            actionType?: v1.EditFavoritesActionType;
                        }
                        // Массив инструментов для редактирования списка избранных инструментов.
                        export interface EditFavoritesRequestInstrument {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Идентификатор инструмента — &#x60;figi&#x60; или &#x60;instrument_uid&#x60;.
                            instrumentId?: string;
                        }
                        // Тип действия со списком избранных инструментов.
                        export enum EditFavoritesActionType {
                            // Тип не определен.
                            EDIT_FAVORITES_ACTION_TYPE_UNSPECIFIED = 0,
                            // Добавить в список.
                            EDIT_FAVORITES_ACTION_TYPE_ADD = 1,
                            // Удалить из списка.
                            EDIT_FAVORITES_ACTION_TYPE_DEL = 2,
                        }
                        // Результат редактирования списка избранных инструментов.
                        export interface EditFavoritesResponse {
                            // Массив инструментов.
                            favoriteInstruments?: v1.FavoriteInstrument[];
                        }
                        // Реальная площадка исполнения расчетов.
                        export enum RealExchange {
                            // Тип не определен.
                            REAL_EXCHANGE_UNSPECIFIED = 0,
                            // Московская биржа.
                            REAL_EXCHANGE_MOEX = 1,
                            // Санкт-Петербургская биржа.
                            REAL_EXCHANGE_RTS = 2,
                            // Внебиржевой инструмент.
                            REAL_EXCHANGE_OTC = 3,
                            // Инструмент, торгуемый на площадке брокера.
                            REAL_EXCHANGE_DEALER = 4,
                        }
                        // tslint:disable-next-line:no-empty-interface
                        // Запрос справочника стран.
                        export interface GetCountriesRequest {
                        }
                        // Справочник стран.
                        export interface GetCountriesResponse {
                            // Массив стран.
                            countries?: v1.CountryResponse[];
                        }
                        // tslint:disable-next-line:no-empty-interface
                        // Запрос справочника индексов и товаров
                        export interface IndicativesRequest {
                        }
                        // Справочник индексов и товаров
                        export interface IndicativesResponse {
                            // Массив инструментов.
                            instruments?: v1.IndicativeResponse[];
                        }
                        // Индикатив
                        export interface IndicativeResponse {
                            // FIGI-идентификатор инструмента.
                            figi?: string;
                            // Тикер инструмента.
                            ticker?: string;
                            // Класс-код инструмента.
                            classCode?: string;
                            // Валюта расчетов.
                            currency?: string;
                            // Тип инструмента.
                            instrumentKind?: v1.InstrumentType;
                            // Название инструмента.
                            name?: string;
                            // Tорговая площадка (секция биржи).
                            exchange?: string;
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Признак доступности для покупки.
                            buyAvailableFlag?: boolean;
                            // Признак доступности для продажи.
                            sellAvailableFlag?: boolean;
                        }
                        // Данные о стране.
                        export interface CountryResponse {
                            // Двухбуквенный код страны.
                            alfaTwo?: string;
                            // Трехбуквенный код страны.
                            alfaThree?: string;
                            // Наименование страны.
                            name?: string;
                            // Краткое наименование страны.
                            nameBrief?: string;
                        }
                        // Запрос на поиск инструментов.
                        export interface FindInstrumentRequest {
                            // Строка поиска.
                            query?: string;
                            // Фильтр по типу инструмента.
                            instrumentKind?: v1.InstrumentType;
                            // Фильтр для отображения только торговых инструментов.
                            apiTradeAvailableFlag?: boolean;
                        }
                        // Результат поиска инструментов.
                        export interface FindInstrumentResponse {
                            // Массив инструментов, удовлетворяющих условиям поиска.
                            instruments?: v1.InstrumentShort[];
                        }
                        // Краткая информация об инструменте.
                        export interface InstrumentShort {
                            // ISIN инструмента.
                            isin?: string;
                            // FIGI инструмента.
                            figi?: string;
                            // Ticker инструмента.
                            ticker?: string;
                            // ClassCode инструмента.
                            classCode?: string;
                            // Тип инструмента.
                            instrumentType?: string;
                            // Название инструмента.
                            name?: string;
                            // Уникальный идентификатор инструмента.
                            uid?: string;
                            // Уникальный идентификатор позиции инструмента.
                            positionUid?: string;
                            // Тип инструмента.
                            instrumentKind?: v1.InstrumentType;
                            // Возможность торговать инструментом через API.
                            apiTradeAvailableFlag?: boolean;
                            // Признак доступности для ИИС.
                            forIisFlag?: boolean;
                            // Дата первой минутной свечи.
                            first_1minCandleDate?: google.protobuf.Timestamp;
                            // Дата первой дневной свечи.
                            first_1dayCandleDate?: google.protobuf.Timestamp;
                            // Флаг, отображающий доступность торговли инструментом только для квалифицированных инвесторов.
                            forQualInvestorFlag?: boolean;
                            // Флаг, отображающий доступность торговли инструментом по выходным.
                            weekendFlag?: boolean;
                            // Флаг заблокированного ТКС.
                            blockedTcaFlag?: boolean;
                            // Количество бумаг в лоте.
                            lot?: number;
                        }
                        // Запрос списка брендов.
                        export interface GetBrandsRequest {
                            // Настройки пагинации.
                            paging?: v1.Page;
                        }
                        // Запрос бренда.
                        export interface GetBrandRequest {
                            // UID-идентификатор бренда.
                            id?: string;
                        }
                        // Список брендов.
                        export interface GetBrandsResponse {
                            // Массив брендов.
                            brands?: v1.Brand[];
                            // Данные по пагинации.
                            paging?: v1.PageResponse;
                        }
                        // Запрос фундаментальных показателей
                        export interface GetAssetFundamentalsRequest {
                            // Массив идентификаторов активов, не более 100 шт.
                            assets?: string[];
                        }
                        // Фундаментальные показатели
                        export interface GetAssetFundamentalsResponse {
                            fundamentals?: GetAssetFundamentalsResponse.StatisticResponse[];
                        }
                        export namespace GetAssetFundamentalsResponse {
                            // Фундаментальные показатели
                            // Фундаментальные показатели по активу
                            export interface StatisticResponse {
                                // Идентификатор актива.
                                assetUid?: string;
                                // Валюта.
                                currency?: string;
                                // Рыночная капитализация.
                                marketCapitalization?: number;
                                // Максимум за год.
                                highPriceLast_52Weeks?: number;
                                // Минимум за год.
                                lowPriceLast_52Weeks?: number;
                                // Средний объем торгов за 10 дней.
                                averageDailyVolumeLast_10Days?: number;
                                // Средний объем торгов за месяц.
                                averageDailyVolumeLast_4Weeks?: number;
                                beta?: number;
                                // Доля акций в свободном обращении.
                                freeFloat?: number;
                                // Процент форвардной дивидендной доходности по отношению к цене акций.
                                forwardAnnualDividendYield?: number;
                                // Количество акций в обращении.
                                sharesOutstanding?: number;
                                // Выручка.
                                revenueTtm?: number;
                                // EBITDA — прибыль до вычета процентов, налогов, износа и амортизации.
                                ebitdaTtm?: number;
                                // Чистая прибыль.
                                netIncomeTtm?: number;
                                // EPS — величина чистой прибыли компании, которая приходится на каждую обыкновенную акцию.
                                epsTtm?: number;
                                // EPS компании с допущением, что все конвертируемые ценные бумаги компании были сконвертированы в обыкновенные акции.
                                dilutedEpsTtm?: number;
                                // Свободный денежный поток.
                                freeCashFlowTtm?: number;
                                // Среднегодовой  рocт выручки за 5 лет.
                                fiveYearAnnualRevenueGrowthRate?: number;
                                // Среднегодовой  рocт выручки за 3 года.
                                threeYearAnnualRevenueGrowthRate?: number;
                                // Соотношение рыночной капитализации компании к ее чистой прибыли.
                                peRatioTtm?: number;
                                // Соотношение рыночной капитализации компании к ее выручке.
                                priceToSalesTtm?: number;
                                // Соотношение рыночной капитализации компании к ее балансовой стоимости.
                                priceToBookTtm?: number;
                                // Соотношение рыночной капитализации компании к ее свободному денежному потоку.
                                priceToFreeCashFlowTtm?: number;
                                // Рыночная стоимость компании.
                                totalEnterpriseValueMrq?: number;
                                // Соотношение EV и EBITDA.
                                evToEbitdaMrq?: number;
                                // Маржа чистой прибыли.
                                netMarginMrq?: number;
                                // Рентабельность чистой прибыли.
                                netInterestMarginMrq?: number;
                                // Рентабельность собственного капитала.
                                roe?: number;
                                // Рентабельность активов.
                                roa?: number;
                                // Рентабельность активов.
                                roic?: number;
                                // Сумма краткосрочных и долгосрочных обязательств компании.
                                totalDebtMrq?: number;
                                // Соотношение долга к собственному капиталу.
                                totalDebtToEquityMrq?: number;
                                // Total Debt/EBITDA.
                                totalDebtToEbitdaMrq?: number;
                                // Отношение свободногоо кэша к стоимости.
                                freeCashFlowToPrice?: number;
                                // Отношение чистого долга к EBITDA.
                                netDebtToEbitda?: number;
                                // Коэффициент текущей ликвидности.
                                currentRatioMrq?: number;
                                // Коэффициент покрытия фиксированных платежей — FCCR.
                                fixedChargeCoverageRatioFy?: number;
                                // Дивидендная доходность за 12 месяцев.
                                dividendYieldDailyTtm?: number;
                                // Выплаченные дивиденды за 12 месяцев.
                                dividendRateTtm?: number;
                                // Значение дивидендов на акцию.
                                dividendsPerShare?: number;
                                // Средняя дивидендная доходность за 5 лет.
                                fiveYearsAverageDividendYield?: number;
                                // Среднегодовой рост дивидендов за 5 лет.
                                fiveYearAnnualDividendGrowthRate?: number;
                                // Процент чистой прибыли, уходящий на выплату дивидендов.
                                dividendPayoutRatioFy?: number;
                                // Деньги, потраченные на обратный выкуп акций.
                                buyBackTtm?: number;
                                // Рост выручки за 1 год.
                                oneYearAnnualRevenueGrowthRate?: number;
                                // Код страны.
                                domicileIndicatorCode?: string;
                                // Соотношение депозитарной расписки к акциям.
                                adrToCommonShareRatio?: number;
                                // Количество сотрудников.
                                numberOfEmployees?: number;
                                exDividendDate?: google.protobuf.Timestamp;
                                // Начало фискального периода.
                                fiscalPeriodStartDate?: google.protobuf.Timestamp;
                                // Окончание фискального периода.
                                fiscalPeriodEndDate?: google.protobuf.Timestamp;
                                // Изменение общего дохода за 5 лет.
                                revenueChangeFiveYears?: number;
                                // Изменение EPS за 5 лет.
                                epsChangeFiveYears?: number;
                                // Изменение EBIDTA за 5 лет.
                                ebitdaChangeFiveYears?: number;
                                // Изменение общей задолжности за 5 лет.
                                totalDebtChangeFiveYears?: number;
                                // Отношение EV к выручке.
                                evToSales?: number;
                            }
                        }
                        // Запрос отчетов эмитентов
                        export interface GetAssetReportsRequest {
                            // Идентификатор инструмента в формате UID.
                            instrumentId?: string;
                            // Начало запрашиваемого периода по UTC.
                            from?: google.protobuf.Timestamp;
                            // Окончание запрашиваемого периода по UTC.
                            to?: google.protobuf.Timestamp;
                        }
                        // Отчеты эмитентов
                        export interface GetAssetReportsResponse {
                            // Массив событий по облигации.
                            events?: GetAssetReportsResponse.GetAssetReportsEvent[];
                        }
                        export namespace GetAssetReportsResponse {
                            // Отчеты эмитентов
                            // Отчет
                            export interface GetAssetReportsEvent {
                                // Идентификатор инструмента.
                                instrumentId?: string;
                                // Дата публикации отчета.
                                reportDate?: google.protobuf.Timestamp;
                                // Год периода отчета.
                                periodYear?: number;
                                // Номер периода.
                                periodNum?: number;
                                // Тип отчета.
                                periodType?: GetAssetReportsResponse.AssetReportPeriodType;
                                // Дата создания записи.
                                createdAt?: google.protobuf.Timestamp;
                            }
                            export enum AssetReportPeriodType {
                                // Не указан.
                                PERIOD_TYPE_UNSPECIFIED = 0,
                                // Квартальный.
                                PERIOD_TYPE_QUARTER = 1,
                                // Полугодовой.
                                PERIOD_TYPE_SEMIANNUAL = 2,
                                // Годовой.
                                PERIOD_TYPE_ANNUAL = 3,
                            }
                        }
                        // Запрос консенсус-прогнозов
                        export interface GetConsensusForecastsRequest {
                            // Настройки пагинации.
                            paging?: v1.Page;
                        }
                        // Консенсус-прогнозы
                        export interface GetConsensusForecastsResponse {
                            // Массив прогнозов.
                            items?: GetConsensusForecastsResponse.ConsensusForecastsItem[];
                            // Данные по пагинации.
                            page?: v1.PageResponse;
                        }
                        export namespace GetConsensusForecastsResponse {
                            // Консенсус-прогнозы
                            // Прогноз
                            export interface ConsensusForecastsItem {
                                // UID-идентификатор.
                                uid?: string;
                                // UID-идентификатор актива.
                                assetUid?: string;
                                // Дата и время создания записи.
                                createdAt?: google.protobuf.Timestamp;
                                // Целевая цена на 12 месяцев.
                                bestTargetPrice?: v1.Quotation;
                                // Минимальная прогнозная цена.
                                bestTargetLow?: v1.Quotation;
                                // Максимальная прогнозная цена.
                                bestTargetHigh?: v1.Quotation;
                                // Количество аналитиков рекомендующих покупать.
                                totalBuyRecommend?: number;
                                // Количество аналитиков рекомендующих держать.
                                totalHoldRecommend?: number;
                                // Количество аналитиков рекомендующих продавать.
                                totalSellRecommend?: number;
                                // Валюта прогнозов инструмента.
                                currency?: string;
                                // Консенсус-прогноз.
                                consensus?: v1.Recommendation;
                                // Дата прогноза.
                                prognosisDate?: google.protobuf.Timestamp;
                            }
                        }
                        export enum Recommendation {
                            // Не определено.
                            RECOMMENDATION_UNSPECIFIED = 0,
                            // Покупать.
                            RECOMMENDATION_BUY = 1,
                            // Держать.
                            RECOMMENDATION_HOLD = 2,
                            // Продавать.
                            RECOMMENDATION_SELL = 3,
                        }
                        // Запрос прогнозов инвестдомов.
                        export interface GetForecastRequest {
                            // Идентификатор инструмента.
                            instrumentId?: string;
                        }
                        // Прогнозы инвестдомов по инструменту.
                        export interface GetForecastResponse {
                            // Массив прогнозов.
                            targets?: GetForecastResponse.TargetItem[];
                            // Согласованный прогноз.
                            consensus?: GetForecastResponse.ConsensusItem;
                        }
                        export namespace GetForecastResponse {
                            // Прогнозы инвестдомов по инструменту.
                            // Прогноз
                            export interface TargetItem {
                                // Уникальный идентификатор инструмента.
                                uid?: string;
                                // Тикер инструмента.
                                ticker?: string;
                                // Название компании, давшей прогноз.
                                company?: string;
                                // Прогноз.
                                recommendation?: v1.Recommendation;
                                // Дата прогноза.
                                recommendationDate?: google.protobuf.Timestamp;
                                // Валюта.
                                currency?: string;
                                // Текущая цена.
                                currentPrice?: v1.Quotation;
                                // Прогнозируемая цена.
                                targetPrice?: v1.Quotation;
                                // Изменение цены.
                                priceChange?: v1.Quotation;
                                // Относительное изменение цены.
                                priceChangeRel?: v1.Quotation;
                                // Наименование инструмента.
                                showName?: string;
                            }
                            // Консенсус-прогноз.
                            export interface ConsensusItem {
                                // Уникальный идентификатор инструмента.
                                uid?: string;
                                // Тикер инструмента.
                                ticker?: string;
                                // Прогноз.
                                recommendation?: v1.Recommendation;
                                // Валюта.
                                currency?: string;
                                // Текущая цена.
                                currentPrice?: v1.Quotation;
                                // Прогнозируемая цена.
                                consensus?: v1.Quotation;
                                // Минимальная цена прогноза.
                                minTarget?: v1.Quotation;
                                // Максимальная цена прогноза.
                                maxTarget?: v1.Quotation;
                                // Изменение цены.
                                priceChange?: v1.Quotation;
                                // Относительное изменение цены.
                                priceChangeRel?: v1.Quotation;
                            }
                        }
                        export interface TradingInterval {
                            // Название интервала.
                            type?: string;
                            // Интервал.
                            interval?: TradingInterval.TimeInterval;
                        }
                        export namespace TradingInterval {
                            export interface TimeInterval {
                                // Время начала интервала.
                                startTs?: google.protobuf.Timestamp;
                                // Время окончания интервала.
                                endTs?: google.protobuf.Timestamp;
                            }
                        }
                        // Уровень риска облигации.
                        export enum RiskLevel {
                            // Не указан.
                            RISK_LEVEL_UNSPECIFIED = 0,
                            // Низкий уровень риска.
                            RISK_LEVEL_LOW = 1,
                            // Средний уровень риска.
                            RISK_LEVEL_MODERATE = 2,
                            // Высокий уровень риска.
                            RISK_LEVEL_HIGH = 3,
                        }
                        export enum BondType {
                            // Тип облигации не определен.
                            BOND_TYPE_UNSPECIFIED = 0,
                            // Замещающая облигация.
                            BOND_TYPE_REPLACED = 1,
                        }
                        // Площадка торговли.
                        export enum InstrumentExchangeType {
                            // Площадка торговли не определена.
                            INSTRUMENT_EXCHANGE_UNSPECIFIED = 0,
                            // Бумага, торгуемая у дилера.
                            INSTRUMENT_EXCHANGE_DEALER = 1,
                        }
                        // Тип инструмента.
                        export enum InstrumentType {
                            INSTRUMENT_TYPE_UNSPECIFIED = 0,
                            // Облигация.
                            INSTRUMENT_TYPE_BOND = 1,
                            // Акция.
                            INSTRUMENT_TYPE_SHARE = 2,
                            // Валюта.
                            INSTRUMENT_TYPE_CURRENCY = 3,
                            // Exchange-traded fund. Фонд.
                            INSTRUMENT_TYPE_ETF = 4,
                            // Фьючерс.
                            INSTRUMENT_TYPE_FUTURES = 5,
                            // Структурная нота.
                            INSTRUMENT_TYPE_SP = 6,
                            // Опцион.
                            INSTRUMENT_TYPE_OPTION = 7,
                            // Clearing certificate.
                            INSTRUMENT_TYPE_CLEARING_CERTIFICATE = 8,
                            // Индекс.
                            INSTRUMENT_TYPE_INDEX = 9,
                            // Товар.
                            INSTRUMENT_TYPE_COMMODITY = 10,
                        }
                        // Статус запрашиваемых инструментов.
                        export enum InstrumentStatus {
                            // Значение не определено.
                            INSTRUMENT_STATUS_UNSPECIFIED = 0,
                            // По умолчанию — базовый список инструментов, которыми можно торговать через T-Invest API. Сейчас списки доступных бумаг в API и других интерфейсах совпадают — кроме внебиржевых бумаг, но в будущем списки могут различаться.
                            INSTRUMENT_STATUS_BASE = 1,
                            // Список всех инструментов.
                            INSTRUMENT_STATUS_ALL = 2,
                        }
                        // Денежная сумма в определенной валюте.
                        export interface MoneyValue {
                            // Строковый ISO-код валюты.
                            currency?: string;
                            // Целая часть суммы, может быть отрицательным числом.
                            units?: number;
                            // Дробная часть суммы, может быть отрицательным числом.
                            nano?: number;
                        }
                        // Котировка — денежная сумма без указания валюты.
                        export interface Quotation {
                            // Целая часть суммы, может быть отрицательным числом.
                            units?: number;
                            // Дробная часть суммы, может быть отрицательным числом.
                            nano?: number;
                        }
                        // Режим торгов инструмента
                        export enum SecurityTradingStatus {
                            // Торговый статус не определен.
                            SECURITY_TRADING_STATUS_UNSPECIFIED = 0,
                            // Недоступен для торгов.
                            SECURITY_TRADING_STATUS_NOT_AVAILABLE_FOR_TRADING = 1,
                            // Период открытия торгов.
                            SECURITY_TRADING_STATUS_OPENING_PERIOD = 2,
                            // Период закрытия торгов.
                            SECURITY_TRADING_STATUS_CLOSING_PERIOD = 3,
                            // Перерыв в торговле.
                            SECURITY_TRADING_STATUS_BREAK_IN_TRADING = 4,
                            // Нормальная торговля.
                            SECURITY_TRADING_STATUS_NORMAL_TRADING = 5,
                            // Аукцион закрытия.
                            SECURITY_TRADING_STATUS_CLOSING_AUCTION = 6,
                            // Аукцион крупных пакетов.
                            SECURITY_TRADING_STATUS_DARK_POOL_AUCTION = 7,
                            // Дискретный аукцион.
                            SECURITY_TRADING_STATUS_DISCRETE_AUCTION = 8,
                            // Аукцион открытия.
                            SECURITY_TRADING_STATUS_OPENING_AUCTION_PERIOD = 9,
                            // Период торгов по цене аукциона закрытия.
                            SECURITY_TRADING_STATUS_TRADING_AT_CLOSING_AUCTION_PRICE = 10,
                            // Сессия назначена.
                            SECURITY_TRADING_STATUS_SESSION_ASSIGNED = 11,
                            // Сессия закрыта.
                            SECURITY_TRADING_STATUS_SESSION_CLOSE = 12,
                            // Сессия открыта.
                            SECURITY_TRADING_STATUS_SESSION_OPEN = 13,
                            // Доступна торговля в режиме внутренней ликвидности брокера.
                            SECURITY_TRADING_STATUS_DEALER_NORMAL_TRADING = 14,
                            // Перерыв торговли в режиме внутренней ликвидности брокера.
                            SECURITY_TRADING_STATUS_DEALER_BREAK_IN_TRADING = 15,
                            // Недоступна торговля в режиме внутренней ликвидности брокера.
                            SECURITY_TRADING_STATUS_DEALER_NOT_AVAILABLE_FOR_TRADING = 16,
                        }
                        export interface PingRequest {
                            // Время формирования запроса.
                            time?: google.protobuf.Timestamp;
                        }
                        export interface PingDelaySettings {
                            // Задержка (пинг) сообщений:  5000–180 000 миллисекунд. Значение по умолчанию — 120 000.
                            pingDelayMs?: number;
                        }
                        // Проверка активности стрима.
                        export interface Ping {
                            // Время проверки.
                            time?: google.protobuf.Timestamp;
                            // Идентификатор соединения.
                            streamId?: string;
                            // Время формирования запроса.
                            pingRequestTime?: google.protobuf.Timestamp;
                        }
                        // Тип цены.
                        export enum PriceType {
                            // Значение не определено.
                            PRICE_TYPE_UNSPECIFIED = 0,
                            // Цена в пунктах (только для фьючерсов и облигаций).
                            PRICE_TYPE_POINT = 1,
                            // Цена в валюте расчетов по инструменту.
                            PRICE_TYPE_CURRENCY = 2,
                        }
                        export interface Page {
                            // Максимальное число возвращаемых записей.
                            limit?: number;
                            // Порядковый номер страницы, начиная с 0.
                            pageNumber?: number;
                        }
                        export interface PageResponse {
                            // Максимальное число возвращаемых записей.
                            limit?: number;
                            // Порядковый номер страницы, начиная с 0.
                            pageNumber?: number;
                            // Общее количество записей.
                            totalCount?: number;
                        }
                        export interface ResponseMetadata {
                            // Идентификатор трекинга.
                            trackingId?: string;
                            // Серверное время.
                            serverTime?: google.protobuf.Timestamp;
                        }
                        export interface BrandData {
                            // Логотип инструмента. Имя файла для получения логотипа.
                            logoName?: string;
                            // Цвет бренда.
                            logoBaseColor?: string;
                            // Цвет текста для цвета логотипа бренда.
                            textColor?: string;
                        }
                        export enum ResultSubscriptionStatus {
                            // Статус подписки не определен.
                            RESULT_SUBSCRIPTION_STATUS_UNSPECIFIED = 0,
                            // Подписка успешно установлена.
                            RESULT_SUBSCRIPTION_STATUS_OK = 1,
                            // Ошибка подписки
                            RESULT_SUBSCRIPTION_STATUS_ERROR = 13,
                        }
                        export interface ErrorDetail {
                            // Код ошибки.
                            code?: string;
                            // Описание ошибки.
                            message?: string;
                        }
                    }
                }
            }
        }
    }
}
export namespace google {
    export namespace protobuf {
        export interface Timestamp {
            seconds?: number;
            nanos?: number;
        }
    }
}

