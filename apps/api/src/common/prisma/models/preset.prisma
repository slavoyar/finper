model Preset {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  name        String
  type        String
  riskLevels  Int[]
  minDuration Int?
  maxDuration Int?
  count       Int?
  userId      Int
}
